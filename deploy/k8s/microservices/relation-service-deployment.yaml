apiVersion: apps/v1
kind: Deployment
metadata:
  name: relation-service
  namespace: chat-system
  labels:
    app: relation-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: relation-service
  template:
    metadata:
      labels:
        app: relation-service
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - relation-service
                topologyKey: "kubernetes.io/hostname"
      containers:
        - name: relation-service
          image: myregistry.com/relation-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 50053
          env:
            - name: ETCD_ENDPOINTS
              value: "etcd:2379"
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            tcpSocket:
              port: 50053
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 50053
            initialDelaySeconds: 15
            periodSeconds: 20
          volumeMounts:
            - name: config-volume
              mountPath: /app/configs/config.toml
              subPath: config.toml

      volumes:
        - name: config-volume
          configMap:
            name: chat-config

---
apiVersion: v1
kind: Service
metadata:
  name: relation-service
  namespace: chat-system
spec:
  selector:
    app: relation-service
  ports:
    - protocol: TCP
      port: 50053
      targetPort: 50053
