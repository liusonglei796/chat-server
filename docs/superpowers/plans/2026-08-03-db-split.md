# 数据库拆分三步改造（单服务单库）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将共享单一 MySQL（Shared Database 反模式）拆分为"单服务单库"：user_service 独占 `chat_user`、relation_service 独占 `chat_relation`、message_service 独占 `chat_message`，跨库读走 gRPC、跨库写走事件（Outbox + Kafka），并顺带完成 auth_service 并入 user_service。

**Architecture:**
- **读**：跨库读一律改为 gRPC。relation 查 user 状态 → `user_service.GetUserInfo`；relation 列表拼昵称头像 → `user_service.BatchGetPublicUserInfo`；message/session 校验好友/群成员/用户群组状态 → `relation_service.CheckFriendship` / `CheckGroupMember` / `GetGroupDetail` + `user_service.GetUserInfo`。
- **写**：跨库写 session 的 6 个路径（CreateGroup / ApplyGroup(DIRECT) / PassGroupApply / DismissGroup / UpdateGroupInfo / UpdateUserInfo）改为"本地事务 + outbox 落表 → 发布器轮询发 Kafka `domain_events` → message_service 消费建/改/删 session"。事件消费幂等（先查存在性跳过 + Redis SetNX 去重）。
- **隔离**：同实例 3 库 3 账号（svc_user/svc_relation/svc_message）+ GRANT；AutoMigrate 按服务收敛；config 增加 `MYSQL_USER`/`DB_NAME` 环境变量覆盖；docker-compose 挂载初始化 SQL、移除 auth-service。
- 实现顺序重排为「gRPC 读 → 事件写 → 物理隔离」，因为物理拆库必须等跨库访问全部移除后才能执行。三步都在单库阶段可运行、可回归。

**Tech Stack:** Go 1.26 / GORM / gRPC / etcd 服务发现 / segmentio/kafka-go / MySQL 8.0 / Redis（幂等）/ docker-compose

**交付验证：** 每步 `go build ./...` 与 `go vet ./...` 通过；逻辑层新代码写 Go 单测；端到端回归用 `docker-compose up --build` + `test_all_apis.py`（注册/登录/加好友/建群/进群/发消息/改资料流程）。压测/链路追踪验证按规范需输出 hey 数据与 Tempo trace（当前仓库无独立压测脚本，验收阶段手动执行）。

---

## 前置状态说明（务必先读）

- **当前共享库**：`configs/config.toml` 的 `[mysqlConfig]` = user `root` / password `root123456` / databaseName `chat`。所有服务都连这个库、`mysqlimpl.Init()` 对全部 7 张表 AutoMigrate。
- **关键决策（用户已批准）**：
  - auth_service **并入** user_service（`authpb.AuthServiceServer` 注册到 user_service 的 gRPC server，删除 auth-service 容器，`grpc_client.AuthClient` 改连 `user_service`）。
  - 不引入 Saga；事件消费失败重试即可。
  - PassFriendApply / DeleteFriend / KickUser **不写 session**，无需事件化，只做 gRPC 读替换。
- **已验证事实**（写路径清单据此，勿凭 spec 旧表格）：6 个真实事件化点见 Task 11-16；PassFriendApply 仅 apply+friendship（apply/service.go:485-565），DeleteFriend 仅 friendship+apply（friendship/service.go:180-221），KickUser 仅删 Redis token（user/service.go:182-202）。
- **`internal/service/services.go`（NewServices）是死代码**，无 cmd 引用（已 grep 确认）。拆分后单服务无法再拿到全部 repo，Task 21 删除之。
- **admin 服务目前 `NewAdminHandler(nil, nil)`**（handlers.go:35），未接真服务。本计划不修复 admin，只保证其不破坏编译。
- **工作区有未提交的旧改动**（UOW WithTx 重构、redis.go 调整、各 cmd 加 `rc.Release()` 等），来自上个会话。**不要动它们、不要提交它们**；每个 Task 的 `git add` 只加本次涉及的文件。
- **protoc 插件未安装**（`protoc-gen-go` / `protoc-gen-go-grpc` 不在 PATH）。Task 1 先安装。

---

## File Structure

### 新建
- `api/gen/user/user.pb.go`、`user_grpc.pb.go`（重新生成，含 BatchGetPublicUserInfo）
- `api/gen/relation/relation.pb.go`、`relation_grpc.pb.go`（重新生成，含 CheckFriendship/CheckGroupMember）
- `internal/domain/store/outbox_store.go` — OutboxStore 接口
- `internal/dao/mysql/outbox/outbox_store.go` — GORM 实现
- `internal/dao/mysql/init_mysql.go`（改：支持按服务迁移指定模型）
- `internal/model/outbox.go` — Outbox 模型
- `internal/dto/event/domain_event.go` — 事件 DTO 与类型常量
- `pkg/outbox/publisher.go` — Outbox 发布器（轮询 + 发 Kafka + 标记已发布）
- `pkg/outbox/kafka.go` — domain_events 生产者封装
- `internal/service/message/domain_event_consumer.go` — message_service 事件消费者
- `internal/service/message/session_event_handler.go` — 事件处理器（建/改/删 session）
- `migrations/split_db.sql` — 建 3 库 3 账号 + GRANT
- `migrations/split_migrate.sql` — 旧库数据搬移到新库（一次性脚本）

### 修改
- `api/proto/user.proto` — 新增 BatchGetPublicUserInfo
- `api/proto/relation.proto` — 新增 CheckFriendship / CheckGroupMember
- `internal/service/user/grpc_server.go` — 实现 BatchGetPublicUserInfo
- `internal/service/user/service.go` — 增加 BatchGetPublicUserInfo 方法
- `internal/grpc_client/client.go` — AuthClient 改连 user_service；增加读封装函数
- `internal/service/auth/grpc_server.go` + `service.go` — 迁入 user_service（包不变，注册点变化）
- `internal/service/apply/service.go` — R1 读替换（ApplyFriend:65 / PassFriendApply:508）；W2/W3 事件化（247/651 建 session 移除）
- `internal/service/friendship/service.go` — R2 读替换（GetFriendList:89-107 / GetFriendInfo:135-139）
- `internal/service/group/service.go` — R2 读替换（GetGroupMemberList 成员信息）；W1/W4/W5 事件化（74/296/377）
- `internal/service/session/service.go` — R1b 读替换（CreateSession/OpenSession/CheckOpenSessionAllowed/checkTargetStatusWithCache）
- `internal/service/message/service.go` + `grpc_server.go` — R1b 读替换（IsFriend → gRPC）
- `internal/service/message/kafka_processor.go` — R1b 读替换（checkSendPermission / getGroupMembersCached）
- `internal/config/config.go` — overlayEnvVars 增 MYSQL_USER/DB_NAME
- `configs/config.toml` —（保持单库默认，环境变量覆盖进隔离阶段）
- `cmd/user_service/main.go` — auth 并入、配置 env、AutoMigrate 收敛
- `cmd/relation_service/main.go` — 配置 env、AutoMigrate 收敛、outbox 启动
- `cmd/message_service/main.go` — 配置 env、AutoMigrate 收敛、事件消费者启动
- `cmd/chat_server/main.go` — AuthClient 指向不变（服务名改 user_service）
- `internal/dao/mysql/stores.go` — 加 OutboxRepo 字段
- `docker-compose.yml` — mysql 挂载 split_db.sql、各服务 env、移除 auth-service
- `internal/service/services.go` — 删除（死代码）

---

## Phase 1: gRPC 读 API 化

### Task 1: 安装 protoc 插件 + 扩展 proto 文件并重新生成

**Files:**
- Modify: `api/proto/user.proto`
- Modify: `api/proto/relation.proto`
- Regenerate: `api/gen/user/*`, `api/gen/relation/*`

- [ ] **Step 1: 安装 protoc 插件**

Run:
```bash
go install google.golang.org/protobuf/cmd/protoc-gen-go@v1.36.6
go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@v1.5.1
export PATH="$PATH:$(go env GOPATH)/bin"
```
Expected: both install without error.

- [ ] **Step 2: 修改 `api/proto/user.proto`，新增批量接口**

在 `GetPublicUserInfoResponse` 消息后追加：
```proto
rpc BatchGetPublicUserInfo (BatchGetPublicUserInfoRequest) returns (BatchGetPublicUserInfoResponse);

message BatchGetPublicUserInfoRequest {
    repeated string user_ids = 1;
}
message BatchGetPublicUserInfoResponse {
    repeated PublicUserInfo users = 1;
}

message PublicUserInfo {
    string uuid = 1;
    string nickname = 2;
    string avatar = 3;
    int32 gender = 4;
    string birthday = 5;
    string signature = 6;
}
```

- [ ] **Step 3: 修改 `api/proto/relation.proto`，新增两个校验方法**

在 `service RelationService` 块内追加：
```proto
    // 检查好友关系状态：0=非好友 1=正常 2=已拉黑对方 3=被对方拉黑
    rpc CheckFriendship (CheckFriendshipRequest) returns (CheckFriendshipResponse);
    // 检查用户是否为群成员
    rpc CheckGroupMember (CheckGroupMemberRequest) returns (CheckGroupMemberResponse);
    // 获取群全部成员ID（供消息服务群发用，不要求调用方是成员）
    rpc ListGroupMemberIds (ListGroupMemberIdsRequest) returns (ListGroupMemberIdsResponse);
```
在文件末尾追加消息定义：
```proto
message CheckFriendshipRequest {
    string user_id = 1;
    string friend_id = 2;
}
message CheckFriendshipResponse {
    int32 status = 1;
}

message CheckGroupMemberRequest {
    string group_id = 1;
    string user_id = 2;
}
message CheckGroupMemberResponse {
    bool is_member = 1;
}

message ListGroupMemberIdsRequest {
    string group_id = 1;
}
message ListGroupMemberIdsResponse {
    repeated string user_ids = 1;
}
```

- [ ] **Step 4: 重新生成 pb 代码**

Run:
```bash
export PATH="$PATH:$(go env GOPATH)/bin"
cd api/proto
protoc --go_out=../gen --go-grpc_out=../gen \
  --go_opt=module=kama_chat_server/api/gen \
  --go-grpc_opt=module=kama_chat_server/api/gen \
  user.proto relation.proto
cd ../..
```
Expected: `api/gen/user/user.pb.go` 含 `BatchGetPublicUserInfo` 与 `PublicUserInfo`；`api/gen/relation/relation.pb.go` 含 `CheckFriendship`/`CheckGroupMember`/`ListGroupMemberIds`。生成后跑 `go build ./...` 应报错（UserServiceServer/RelationServiceServer 实现不完整），这是预期的红——Task 2/3 补实现。

- [ ] **Step 5: 提交**

```bash
git add api/proto/user.proto api/proto/relation.proto api/gen/user api/gen/relation
git commit -m "feat(proto): add BatchGetPublicUserInfo and CheckFriendship/CheckGroupMember"
```

---

### Task 2: user_service 实现 BatchGetPublicUserInfo

**Files:**
- Modify: `internal/service/user/service.go`
- Modify: `internal/service/user/grpc_server.go`

- [ ] **Step 1: 写失败测试** — `internal/service/user/service_test.go`

```go
package user

import (
	"context"
	"testing"

	"kama_chat_server/internal/dto/respond/user"
	"kama_chat_server/internal/model"
)

type fakeUserRepo struct {
	users map[string]model.UserInfo
}

func (f *fakeUserRepo) FindByUuids(ctx context.Context, uuids []string) ([]model.UserInfo, error) {
	var out []model.UserInfo
	for _, u := range uuids {
		if v, ok := f.users[u]; ok {
			out = append(out, v)
		}
	}
	return out, nil
}

func TestBatchGetPublicUserInfo_FiltersNotFound(t *testing.T) {
	svc := &UserService{uow: &fakeUOW{userRepo: &fakeUserRepo{users: map[string]model.UserInfo{
		"U1": {Uuid: "U1", Nickname: "a", Avatar: "av1"},
		"U3": {Uuid: "U3", Nickname: "c", Avatar: "av3"},
	}}}}

	got, err := svc.BatchGetPublicUserInfo(context.Background(), []string{"U1", "U2", "U3"})
	if err != nil {
		t.Fatalf("unexpected error: %v", err)
	}
	want := map[string]string{"U1": "a", "U3": "c"}
	if len(got) != 2 {
		t.Fatalf("want 2 results, got %d", len(got))
	}
	for _, g := range got {
		if name, ok := want[g.Uuid]; !ok || g.Nickname != name {
			t.Errorf("unexpected user: %+v", g)
		}
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `go test ./internal/service/user/ -run TestBatchGetPublicUserInfo -v`
Expected: 编译错误（`fakeUOW`、`BatchGetPublicUserInfo` 未定义）。

- [ ] **Step 3: 实现测试替身 UOW 与批量方法**

在 `internal/service/user/service_test.go` 追加：
```go
type fakeUOW struct {
	userRepo *fakeUserRepo
}

func (f *fakeUOW) UserStore() store.UserStore   { return f.userRepo }
func (f *fakeUOW) GroupStore() store.GroupStore { return nil }
func (f *fakeUOW) FriendshipStore() store.FriendshipStore { return nil }
func (f *fakeUOW) SessionStore() store.SessionStore { return nil }
func (f *fakeUOW) MessageStore() store.MessageStore { return nil }
func (f *fakeUOW) ApplyStore() store.ApplyStore { return nil }
func (f *fakeUOW) GroupMemberStore() store.GroupMemberStore { return nil }
func (f *fakeUOW) WithTx(fn func(store.UnitOfWork) error) error { return fn(f) }
```
（`import "kama_chat_server/internal/domain/store"`）

在 `internal/service/user/service.go` 新增方法（复用已有 `FindByUuids`，返回公开字段）：
```go
// BatchGetPublicUserInfo 批量获取公开用户信息（昵称/头像/性别等）
// 供跨服务列表页一次拉取，避免 N+1 调用
func (u *UserService) BatchGetPublicUserInfo(ctx context.Context, userIds []string) ([]userrsp.PublicUserInfoRespond, error) {
	if len(userIds) == 0 {
		return []userrsp.PublicUserInfoRespond{}, nil
	}
	userList, err := u.uow.UserStore().FindByUuids(ctx, userIds)
	if err != nil {
		zap.L().Error("batch find users error", zap.Error(err))
		return nil, errorx.ErrServerBusy
	}
	rsp := make([]userrsp.PublicUserInfoRespond, 0, len(userList))
	for _, usr := range userList {
		rsp = append(rsp, userrsp.PublicUserInfoRespond{
			Uuid:      usr.Uuid,
			Nickname:  usr.Nickname,
			Avatar:    usr.Avatar,
			Gender:    usr.Gender,
			Birthday:  usr.Birthday,
			Signature: usr.Signature,
		})
	}
	return rsp, nil
}
```
确认 `internal/service/user/service.go` 已 import `userrsp "kama_chat_server/internal/dto/respond/user"` 与 `errorx`（已有，GetPublicUserInfo 同文件用到）。

- [ ] **Step 4: 实现 gRPC 方法** — `internal/service/user/grpc_server.go` 追加：
```go
func (s *GrpcServer) BatchGetPublicUserInfo(ctx context.Context, req *userpb.BatchGetPublicUserInfoRequest) (*userpb.BatchGetPublicUserInfoResponse, error) {
	infoList, err := s.svc.BatchGetPublicUserInfo(ctx, req.UserIds)
	if err != nil {
		return nil, err
	}
	users := make([]*userpb.PublicUserInfo, 0, len(infoList))
	for _, info := range infoList {
		users = append(users, &userpb.PublicUserInfo{
			Uuid:      info.Uuid,
			Nickname:  info.Nickname,
			Avatar:    info.Avatar,
			Gender:    int32(info.Gender),
			Birthday:  info.Birthday,
			Signature: info.Signature,
		})
	}
	return &userpb.BatchGetPublicUserInfoResponse{Users: users}, nil
}
```

- [ ] **Step 5: 运行测试确认通过**

Run: `go test ./internal/service/user/ -run TestBatchGetPublicUserInfo -v`
Expected: PASS。再 `go build ./...` 应无报错（user grpc server 补齐后不再缺方法）。

- [ ] **Step 6: 提交**

```bash
git add internal/service/user/service.go internal/service/user/grpc_server.go internal/service/user/service_test.go
git commit -m "feat(user): implement BatchGetPublicUserInfo gRPC for cross-service list reads"
```

---

### Task 3: relation_service 实现 CheckFriendship / CheckGroupMember

**Files:**
- Modify: `internal/service/relation/grpc_server.go`

- [ ] **Step 1: 实现两个 gRPC 方法**（复用 apply 包的黑名单枚举，直接查库）

在 `internal/service/relation/grpc_server.go` 追加：
```go
// CheckFriendship 返回好友关系状态：0=非好友 1=正常 2=已拉黑对方 3=被对方拉黑
func (s *GrpcServer) CheckFriendship(ctx context.Context, req *relationpb.CheckFriendshipRequest) (*relationpb.CheckFriendshipResponse, error) {
	fs, err := s.friendshipSvc.GetFriendshipStatus(ctx, req.UserId, req.FriendId)
	if err != nil {
		return nil, err
	}
	return &relationpb.CheckFriendshipResponse{Status: int32(fs)}, nil
}

// CheckGroupMember 检查用户是否为群成员
func (s *GrpcServer) CheckGroupMember(ctx context.Context, req *relationpb.CheckGroupMemberRequest) (*relationpb.CheckGroupMemberResponse, error) {
	isMember, err := s.groupSvc.IsGroupMember(ctx, req.GroupId, req.UserId)
	if err != nil {
		return nil, err
	}
	return &relationpb.CheckGroupMemberResponse{IsMember: isMember}, nil
}

// ListGroupMemberIds 获取群全部成员ID（不校验调用方身份，供消息服务群发）
func (s *GrpcServer) ListGroupMemberIds(ctx context.Context, req *relationpb.ListGroupMemberIdsRequest) (*relationpb.ListGroupMemberIdsResponse, error) {
	ids, err := s.groupSvc.ListGroupMemberIds(ctx, req.GroupId)
	if err != nil {
		return nil, err
	}
	return &relationpb.ListGroupMemberIdsResponse{UserIds: ids}, nil
}
```

- [ ] **Step 2: 在 friendship service 增加 `GetFriendshipStatus`（含对外状态映射）**

**对外契约**（proto 注释）：`0=非好友 1=正常(NORMAL) 2=已拉黑对方(BLACK) 3=被对方拉黑(BE_BLACK)`。
**内部枚举**（`pkg/enum/friendship/friendship_status`）：`NORMAL=0, BE_BLACK=1, BLACK=2`（已实测）。
映射逻辑：查不到（含软删）→ 0；查到 NORMAL(0) → 1；BE_BLACK(1) → 3；BLACK(2) → 2。

`internal/service/friendship/service.go` 追加：
```go
// GetFriendshipStatus 返回好友关系状态（对外契约）
// 0=非好友 1=正常 2=已拉黑对方 3=被对方拉黑
func (s *FriendshipService) GetFriendshipStatus(ctx context.Context, userId, friendId string) (int8, error) {
	fs, err := s.uow.FriendshipStore().FindByUserIdAndFriendId(ctx, userId, friendId)
	if err != nil {
		if errorx.IsNotFound(err) {
			return 0, nil
		}
		zap.L().Error("query friendship error", zap.Error(err))
		return 0, errorx.ErrServerBusy
	}
	switch fs.Status {
	case friendship_status.BE_BLACK: // 被对方拉黑
		return 3, nil
	case friendship_status.BLACK: // 拉黑对方
		return 2, nil
	default: // NORMAL
		return 1, nil
	}
}
```
补 import `"kama_chat_server/pkg/enum/friendship/friendship_status"`。确认 `errorx` 与 `zap` 已有 import。

- [ ] **Step 3: 在 group service 增加 `IsGroupMember` 与 `ListGroupMemberIds`**

`internal/service/group/service.go` 追加：
```go
// IsGroupMember 检查用户是否为群成员
func (g *GroupService) IsGroupMember(ctx context.Context, groupId, userId string) (bool, error) {
	_, err := g.uow.GroupMemberStore().FindByGroupAndUser(ctx, groupId, userId)
	if err != nil {
		if errorx.IsNotFound(err) {
			return false, nil
		}
		zap.L().Error("query group member error", zap.Error(err))
		return false, errorx.ErrServerBusy
	}
	return true, nil
}

// ListGroupMemberIds 返回群全部成员ID（直接读 group_member 表，不 JOIN user 表）
func (g *GroupService) ListGroupMemberIds(ctx context.Context, groupId string) ([]string, error) {
	members, err := g.uow.GroupMemberStore().FindByGroupUuid(ctx, groupId)
	if err != nil {
		zap.L().Error("query group members error", zap.Error(err))
		return nil, errorx.ErrServerBusy
	}
	ids := make([]string, 0, len(members))
	for _, m := range members {
		ids = append(ids, m.UserUuid)
	}
	return ids, nil
}
```
确认 import 已有 `errorx` 与 `zap`。

- [ ] **Step 4: 构建验证**

Run: `go build ./...`
Expected: 通过（relation grpc server 补齐后不再缺方法）。`go vet ./...` 通过。

- [ ] **Step 5: 提交**

```bash
git add internal/service/relation/grpc_server.go internal/service/friendship/service.go internal/service/group/service.go
git commit -m "feat(relation): implement CheckFriendship/CheckGroupMember gRPC for session validation"
```

---

### Task 4: R1 — relation 服务读 user 状态改为 gRPC

**Files:**
- Modify: `internal/service/apply/service.go`（65 行 ApplyFriend、508 行 PassFriendApply）
- Modify: `internal/grpc_client/client.go`（新增读封装）

- [ ] **Step 1: grpc_client 增加 user 读封装**

`internal/grpc_client/client.go` 追加：
```go
// GetUserStatus 跨服务获取用户状态（0=正常 1=禁用）
// 供 relation/message 服务校验目标用户是否可用
func GetUserStatus(ctx context.Context, targetId string) (int8, error) {
	if UserClient == nil {
		return 0, errorx.New(errorx.CodeServerBusy, "user grpc client not initialized")
	}
	rsp, err := UserClient.GetUserInfo(ctx, &userpb.GetUserInfoRequest{RequesterId: targetId, TargetId: targetId})
	if err != nil {
		return 0, err
	}
	return int8(rsp.Status), nil
}

// BatchGetPublicUserInfo 跨服务批量获取公开用户信息
func BatchGetPublicUserInfo(ctx context.Context, userIds []string) ([]*userpb.PublicUserInfo, error) {
	if UserClient == nil || len(userIds) == 0 {
		return []*userpb.PublicUserInfo{}, nil
	}
	rsp, err := UserClient.BatchGetPublicUserInfo(ctx, &userpb.BatchGetPublicUserInfoRequest{UserIds: userIds})
	if err != nil {
		return nil, err
	}
	return rsp.Users, nil
}
```
补 import：`"kama_chat_server/pkg/errorx"`。若 `errorx.CodeServerBusy` 不存在，改用 `errorx.ErrServerBusy`（先 grep 确认）。

- [ ] **Step 2: ApplyFriend 用 gRPC 校验申请人（目标）用户状态**

`internal/service/apply/service.go` 当前 60-70 行附近：
```go
user, err := u.uow.UserStore().FindByUuid(ctx, req.FriendId)
if err != nil { ... }
if user.Status == user_status.DISABLE { ... }
```
替换为：
```go
status, err := grpc_client.GetUserStatus(ctx, req.FriendId)
if err != nil {
	zap.L().Error("query friend user status via grpc error", zap.Error(err))
	return errorx.ErrServerBusy
}
if status == user_status.DISABLE {
	return errorx.New(errorx.CodeInvalidParam, "对方已被禁用")
}
```
补 import：`"kama_chat_server/internal/grpc_client"`。删除该处对 `u.uow.UserStore()` 的直接引用与多余 `user` 局部变量（`user_status` 常量保留使用）。

- [ ] **Step 3: PassFriendApply 事务内用 gRPC 校验申请人状态**

`internal/service/apply/service.go` 事务内 506-518 行附近：
```go
user, err := tx.UserStore().FindByUuid(ctx, applicantId)
if err != nil { ... }
if user.Status == user_status.DISABLE { ... }
```
替换为（事务内移出，改在事务外先查，事务保持纯 relation 表写）：
```go
status, err := grpc_client.GetUserStatus(ctx, applicantId)
if err != nil {
	zap.L().Error("query applicant status via grpc error", zap.Error(err))
	return errorx.ErrServerBusy
}
if status == user_status.DISABLE {
	return errorx.New(errorx.CodeInvalidParam, "该用户已被禁用")
}
```
放于 `u.uow.WithTx(...)` 之前。事务内不再引用 `UserRepo`。

- [ ] **Step 4: 构建验证**

Run: `go build ./... && go vet ./...`
Expected: 通过。ApplyFriend/PassFriendApply 不再直接读 user 表。

- [ ] **Step 5: 提交**

```bash
git add internal/grpc_client/client.go internal/service/apply/service.go
git commit -m "refactor(relation): read user status via gRPC in ApplyFriend/PassFriendApply"
```

---

### Task 5: R1b — session 服务读 user/group/friendship/group_member 改为 gRPC

> **重要：** message_service 的 `SessionService` 目前持 `userRepo`/`groupRepo`/`groupMemberRepo`/`friendshipRepo`。拆分后这些表不在 message 库。本任务把这些依赖替换为 gRPC 调用，构造签名不变（repo 参数保留但仅 `sessionRepo`/`messageRepo` 使用），后续 Task 21 收尾时再改 main 传参。

**Files:**
- Modify: `internal/service/session/service.go`

- [ ] **Step 1: 替换 `CreateSession` 中的用户校验（87-101 行）**

原逻辑：
```go
_, err = s.userRepo.FindByUuid(ctx, sendId)
if err != nil { ... CodeUserNotExist ... }
```
替换为：
```go
sendStatus, err := grpc_client.GetUserStatus(ctx, sendId)
if err != nil {
	zap.L().Error("query sender via grpc error", zap.Error(err))
	return "", errorx.ErrServerBusy
}
if sendStatus == user_status.DISABLE {
	return "", errorx.New(errorx.CodeUserNotExist, "发送用户不存在")
}
```
（保持外部行为一致：禁用视为不可用。）

- [ ] **Step 2: 替换接收者为用户分支（111-148 行）**

原逻辑：`s.userRepo.FindByUuid(receiveId)` + DISABLE 校验 + `s.friendshipRepo.IsFriend`。
替换为：
```go
if receiveId[0] == 'U' {
	recvStatus, err := grpc_client.GetUserStatus(ctx, receiveId)
	if err != nil {
		zap.L().Error("query receiver via grpc error", zap.Error(err))
		return "", errorx.ErrServerBusy
	}
	if recvStatus == user_status.DISABLE {
		return "", errorx.New(errorx.CodeInvalidParam, "该用户被禁用了")
	}
	isFriend, err := grpc_client.CheckFriendshipStatus(ctx, sendId, receiveId)
	if err != nil {
		zap.L().Error("check friendship via grpc error", zap.Error(err))
		return "", errorx.ErrServerBusy
	}
	if isFriend != 1 {
		return "", errorx.New(errorx.CodeForbidden, "你们还不是好友")
	}
	nickname, avatar, err := grpc_client.GetUserNicknameAvatar(ctx, receiveId)
	if err != nil {
		zap.L().Error("get receiver info via grpc error", zap.Error(err))
		return "", errorx.ErrServerBusy
	}
	session.ReceiveName = nickname
	session.Avatar = avatar
}
```

- [ ] **Step 3: 替换接收者为群分支（149-186 行）**

原逻辑：`s.groupRepo.FindByUuid(receiveId)` + DISABLE 校验 + `s.groupMemberRepo.FindByGroupAndUser`。
**已确认**：relation 的 `GetGroupDetail` 内部已做「成员校验（非成员 → CodeForbidden）+ 群禁用校验（禁用 → CodeInvalidParam）+ 不存在（CodeNotFound）」，且返回 `PublicGroupInfoRespond`（含 Name/Avatar）。因此本分支直接透传 gRPC 错误、用返回的 Name/Avatar 填冗余字段即可：
```go
else {
	group, err := grpc_client.GetGroupDetail(ctx, sendId, receiveId)
	if err != nil {
		zap.L().Error("query group via grpc error", zap.Error(err))
		return "", err // 透传 relation 的 CodeForbidden/CodeInvalidParam/CodeNotFound
	}
	session.ReceiveName = group.GroupName
	session.Avatar = group.GroupAvatar
}
```
无需新增 `group_status` 字段（见 Step 5 说明）。

- [ ] **Step 4: 新增 grpc_client 封装（供 Step 2/3 与 Task 6 用）**

`internal/grpc_client/client.go` 追加：
```go
// CheckFriendshipStatus 跨服务查好友关系状态（0=非好友 1=正常 2=已拉黑对方 3=被对方拉黑）
func CheckFriendshipStatus(ctx context.Context, userId, friendId string) (int8, error) {
	rsp, err := RelationClient.CheckFriendship(ctx, &relationpb.CheckFriendshipRequest{UserId: userId, FriendId: friendId})
	if err != nil {
		return 0, err
	}
	return int8(rsp.Status), nil
}

// CheckGroupMember 跨服务查群成员
func CheckGroupMember(ctx context.Context, groupId, userId string) (bool, error) {
	rsp, err := RelationClient.CheckGroupMember(ctx, &relationpb.CheckGroupMemberRequest{GroupId: groupId, UserId: userId})
	if err != nil {
		return false, err
	}
	return rsp.IsMember, nil
}

// GetGroupDetail 跨服务获取群详情（已含成员/禁用/存在性校验）
func GetGroupDetail(ctx context.Context, userId, groupId string) (*relationpb.GetGroupDetailResponse, error) {
	rsp, err := RelationClient.GetGroupDetail(ctx, &relationpb.GetGroupDetailRequest{UserId: userId, GroupId: groupId})
	if err != nil {
		return nil, err
	}
	return rsp, nil
}
```

- [ ] **Step 5: 不需要扩展 GetGroupDetail 返回群状态（记录原因）**

原计划考虑给 `GetGroupDetailResponse` 加 `group_status` 字段以支持"群 DISABLE"拦截；但实测 relation `GetGroupDetail`（group/service.go:521-534）**已内置**禁用校验（`grp.Status == group_status.DISABLE` → CodeInvalidParam），且成员校验先于状态校验，与原始 session 分支的校验次序一致。故 **skip 此步**：不改 proto、不重新生成、不填字段。`checkTargetStatusWithCache` 的群分支同理直接复用 `GetGroupDetail` 的错误语义。

- [ ] **Step 6: 替换 `checkTargetStatusWithCache` 的数据源（266-341 行）**

loader 里 `s.userRepo.FindByUuid` 与 `s.groupRepo.FindByUuid` 替换为 gRPC：
```go
func(loaderCtx context.Context) (interface{}, error) {
	status, err := grpc_client.GetUserStatus(loaderCtx, targetId)
	if err != nil {
		return nil, errorx.ErrServerBusy
	}
	return user.GetUserInfoRespond{Uuid: targetId, Status: status}, nil
}
```
群分支同理用 `GetGroupDetail` 拿存在性（err 含 CodeNotFound/CodeForbidden），`checkTargetStatusWithCache` 内的 DISABLE 判定改为直接依赖 gRPC 返回（GetGroupDetail 已内置禁用拦截），用户分支保留 status 判定。

- [ ] **Step 7: 替换 `CheckOpenSessionAllowed`（208-263 行）**

- 用户分支：`s.friendshipRepo.FindByUserIdAndFriendId` → `grpc_client.CheckFriendshipStatus`。非好友(status==0) → CodeForbidden"你们还不是好友"；BE_BLACK(3) → "已被对方拉黑"；BLACK(2) → "已拉黑对方"。
- 群分支：`s.groupMemberRepo.FindByGroupAndUser` → `grpc_client.CheckGroupMember`，非成员 → CodeForbidden。
- 目标状态：保留 `checkTargetStatusWithCache`（已改 gRPC）。

- [ ] **Step 8: grpc_client 增加 `GetUserNicknameAvatar`（Step 2/8 用到）**

`internal/grpc_client/client.go` 追加：
```go
// GetUserNicknameAvatar 跨服务取用户昵称头像
func GetUserNicknameAvatar(ctx context.Context, targetId string) (nickname, avatar string, err error) {
	if UserClient == nil {
		return "", "", errorx.New(errorx.CodeServerBusy, "user grpc client not initialized")
	}
	rsp, err := UserClient.GetUserInfo(ctx, &userpb.GetUserInfoRequest{RequesterId: targetId, TargetId: targetId})
	if err != nil {
		return "", "", err
	}
	return rsp.Nickname, rsp.Avatar, nil
}
```
（原逻辑这些值来自 session 冗余写入时的用户查询；gRPC 拿同名数据即可，保持行为一致。此函数同时被 Task 8 的 `GetFriendInfo` 复用。）

- [ ] **Step 9: 构建验证**

Run: `go build ./... && go vet ./...`
Expected: 通过。`s.userRepo`/`s.groupRepo`/`s.groupMemberRepo`/`s.friendshipRepo` 字段仅剩声明未用——**不要删字段**（Task 21 收尾时改构造），但需在结构体加注释标注"拆分后废弃，仅兼容构造签名"。

- [ ] **Step 10: 提交**

```bash
git add internal/service/session/service.go internal/grpc_client/client.go api/proto/relation.proto api/gen/relation internal/service/relation/grpc_server.go
git commit -m "refactor(session): validate sender/receiver/friendship/group via gRPC instead of cross-db reads"
```

---

### Task 6: R1b — message 服务好友校验改为 gRPC

**Files:**
- Modify: `internal/service/message/service.go`（74、174 行 IsFriend）

- [ ] **Step 1: 替换 GetMessageList / GetMessageListCursor 的 IsFriend**

两处原逻辑：
```go
isFriend, err := m.friendshipRepo.IsFriend(ctx, requesterId, partnerId)
if err != nil { ... }
if !isFriend { ... CodeForbidden ... }
```
替换为：
```go
status, err := grpc_client.CheckFriendshipStatus(ctx, requesterId, partnerId)
if err != nil {
	zap.L().Error("check friendship via grpc error", zap.Error(err))
	return nil, 0, errorx.ErrServerBusy
}
if status != 1 {
	return nil, 0, errorx.New(errorx.CodeForbidden, "你们不是好友，无法查看聊天记录")
}
```
补 import `"kama_chat_server/internal/grpc_client"`。

- [ ] **Step 2: 构建验证**

Run: `go build ./... && go vet ./...`
Expected: 通过。

- [ ] **Step 3: 提交**

```bash
git add internal/service/message/service.go
git commit -m "refactor(message): check friendship via gRPC in message list reads"
```

---

### Task 7: R1b — kafka_processor 权限校验与群成员改为 gRPC

**Files:**
- Modify: `internal/service/message/kafka_processor.go`

- [ ] **Step 1: 替换 `checkSendPermission`（192-231 行）**

原逻辑：`k.userRepo.FindByUuid`（发送者状态）、`k.friendshipRepo.IsFriend`、`k.groupMemberRepo.FindByGroupAndUser`。
替换为：
```go
func (k *KafkaProcessor) checkSendPermission(sendId, receiveId string) error {
	ctx := context.Background()
	if len(receiveId) == 0 {
		return fmt.Errorf("接收者ID不能为空")
	}

	if k.userRepo != nil { // 保留为降级开关；grpc 失败时按旧行为放行
		senderStatus, err := grpc_client.GetUserStatus(ctx, sendId)
		if err == nil && senderStatus == user_status.DISABLE {
			return fmt.Errorf("您的账号已被禁用，无法发送消息")
		}
	}

	if receiveId[0] == 'U' {
		status, err := grpc_client.CheckFriendshipStatus(ctx, sendId, receiveId)
		if err == nil && status != 1 {
			return fmt.Errorf("你们还不是好友，无法发送消息")
		}
	} else if receiveId[0] == 'G' {
		isMember, err := grpc_client.CheckGroupMember(ctx, receiveId, sendId)
		if err == nil && !isMember {
			return fmt.Errorf("你不是该群成员，无法发送消息")
		}
	}
	return nil
}
```
补 import `"kama_chat_server/internal/grpc_client"`。

- [ ] **Step 2: 替换 `getGroupMembersCached`（490-511 行）**

原逻辑：`k.groupMemberRepo.FindByGroupUuid`。
**不要用 `GetGroupMemberList` 代替**（它要求调用方是群成员、JOIN user 表、且 pageSize≤100）——用 Task 1/3 新增的 `ListGroupMemberIds`。`internal/grpc_client/client.go` 追加：
```go
// ListGroupMemberIds 跨服务获取群全部成员ID（不要求调用方是成员）
func ListGroupMemberIds(ctx context.Context, groupId string) ([]string, error) {
	if RelationClient == nil {
		return nil, errorx.New(errorx.CodeServerBusy, "relation grpc client not initialized")
	}
	rsp, err := RelationClient.ListGroupMemberIds(ctx, &relationpb.ListGroupMemberIdsRequest{GroupId: groupId})
	if err != nil {
		return nil, err
	}
	return rsp.UserIds, nil
}
```
`kafka_processor.go` 改造：
```go
func (k *KafkaProcessor) getGroupMemberIds(groupId string) []string {
	ctx := context.Background()
	ids, err := grpc_client.ListGroupMemberIds(ctx, groupId)
	if err != nil {
		zap.L().Error("get group members via grpc error", zap.Error(err))
		return nil
	}
	return ids
}
```
补 import `"kama_chat_server/internal/grpc_client"`。

- [ ] **Step 3: `dispatchToGroup` 改用成员 id 列表**

`dispatchToGroup` 中 `groupMembers := k.getGroupMembersCached(message.ReceiveId)` 及后续 `for _, gm := range groupMembers { trySendBack(k, gm.UserUuid, ...) }` 改为：
```go
memberIds := k.getGroupMemberIds(message.ReceiveId)
for _, uid := range memberIds {
	trySendBack(k, uid, message.Uuid, jsonMessage)
}
```
删除不再使用的 `getGroupMembersCached` 与 `k.cacheHelper`（若仅此处用）。

- [ ] **Step 4: 构建验证**

Run: `go build ./... && go vet ./...`
Expected: 通过。`k.userRepo`/`k.friendshipRepo`/`k.groupMemberRepo` 仅剩构造传参（Task 21 收尾），结构体字段保留并标注废弃。

- [ ] **Step 5: 提交**

```bash
git add internal/service/message/kafka_processor.go
git commit -m "refactor(message): validate permission and fetch group members via gRPC in kafka processor"
```

---

### Task 8: R2 — relation 列表方法用 BatchGetPublicUserInfo 拼用户信息

**Files:**
- Modify: `internal/service/friendship/service.go`（GetFriendList:89-107、GetFriendInfo:135-139）
- Modify: `internal/service/apply/service.go`（GetFriendApplyList:348、GetGroupApplyList:446）
- Modify: `internal/service/group/service.go`（GetGroupMemberList 成员信息）

- [ ] **Step 1: GetFriendList 批量 gRPC 取昵称头像**

原逻辑：循环 `cacheHelper.GetOrLoad` + `UserStore().FindByUuid`。
替换为收集 friendId 后一次批量调用：
```go
friendIds := make([]string, 0, len(friendships))
for _, fs := range friendships {
	friendIds = append(friendIds, fs.FriendId)
}
userList, err := grpc_client.BatchGetPublicUserInfo(ctx, friendIds)
if err != nil {
	zap.L().Error("batch get friends info via grpc error", zap.Error(err))
	return []userrsp.MyUserListRespond{}, total, errorx.ErrServerBusy
}
userMap := make(map[string]*userpb.PublicUserInfo, len(userList))
for _, u := range userList {
	userMap[u.Uuid] = u
}
userListRsp := make([]userrsp.MyUserListRespond, 0, len(friendships))
for _, fs := range friendships {
	if u, ok := userMap[fs.FriendId]; ok {
		userListRsp = append(userListRsp, userrsp.MyUserListRespond{
			UserId:   fs.FriendId,
			UserName: u.Nickname,
			Avatar:   u.Avatar,
		})
	}
}
```
补 import：`userpb "kama_chat_server/api/gen/user"`、`"kama_chat_server/internal/grpc_client"`。删除该函数内 `cacheHelper` 循环。

- [ ] **Step 2: GetFriendInfo 的昵称头像改 gRPC**

`GetFriendInfo` 中 `cacheHelper.GetOrLoad` + `FindByUuid`（135-139）替换为：
```go
nickname, avatar, err := grpc_client.GetUserNicknameAvatar(ctx, friendId)
if err != nil {
	zap.L().Error("get friend info via grpc error", zap.Error(err))
	return friendshiprsp.FriendInfoRespond{}, errorx.ErrServerBusy
}
```
组装响应时用 `nickname`/`avatar` 替换原 `user.Nickname`/`user.Avatar`（其余字段若原逻辑有 birthday/email 等敏感字段，保持原返回结构不变；若无 gRPC 等价字段，降级填空）。

- [ ] **Step 3: GetFriendApplyList / GetGroupApplyList 批量 gRPC**

两处 `u.uow.UserStore().FindByUuids(ctx, userUuids)`（348、446）替换为：
```go
userList, err := grpc_client.BatchGetPublicUserInfo(ctx, userUuids)
if err != nil {
	zap.L().Error("batch get applicants via grpc error", zap.Error(err))
	return nil, errorx.ErrServerBusy
}
userMap := make(map[string]*userpb.PublicUserInfo, len(userList))
for _, u := range userList {
	userMap[u.Uuid] = u
}
```
后续组装列表改用 `userMap[apply.ApplicantId].Nickname/.Avatar`。

- [ ] **Step 4: GetGroupMemberList 成员信息改 gRPC**

`group/service.go` 的 `GetGroupMemberList` 当前用 `FindMembersWithUserInfoPaged`（JOIN user 表）。改为：
1. 用 `GroupMemberStore().FindByGroupUuid` 取成员（仅 group_member 表）；
2. 收集 `UserUuid` → `grpc_client.BatchGetPublicUserInfo` 拼 nickname/avatar；
3. 按原分页（page/pageSize）切片返回，total 用成员总数。
请先 `sed -n` 查看 `GetGroupMemberList` 当前实现再替换，保持返回结构 `GetGroupMemberListRespond{UserId, Nickname, Avatar}` 与 total 语义不变。

- [ ] **Step 5: 构建验证**

Run: `go build ./... && go vet ./...`
Expected: 通过。relation 服务不再直接读 user 表。

- [ ] **Step 6: 提交**

```bash
git add internal/service/friendship/service.go internal/service/apply/service.go internal/service/group/service.go
git commit -m "refactor(relation): batch fetch user profile via gRPC in list methods"
```

---

### Task 9: R7 — auth_service 并入 user_service

**Files:**
- Modify: `cmd/user_service/main.go`
- Modify: `internal/grpc_client/client.go`
- Delete: `cmd/auth_service/main.go`（Dockerfile 引用同步删）
- Modify: `docker-compose.yml`（删 auth-service 段）
- Modify: `cmd/chat_server/main.go`（注释更新，代码不变）

- [ ] **Step 1: user_service 同时注册 AuthServiceServer**

`cmd/user_service/main.go`：
```go
authpb "kama_chat_server/api/gen/auth"
"kama_chat_server/internal/service/auth"
...
userSvc := user.NewUserService(repos, cachePort)
authSvc := auth.NewAuthService(cachePort, repos.User)
authGrpcServer := auth.NewGrpcServer(authSvc, userSvc)
...
userpb.RegisterUserServiceServer(s, grpcServer)
authpb.RegisterAuthServiceServer(s, authGrpcServer)
```

- [ ] **Step 2: grpc_client.AuthClient 改连 user_service**

`internal/grpc_client/client.go`：
```go
authConn, err := grpc.Dial(discovery.BuildDialTarget("auth_service"), opts...)
```
改为：
```go
authConn, err := grpc.Dial(discovery.BuildDialTarget("user_service"), opts...)
```

- [ ] **Step 3: 删除 auth-service 容器定义**

`docker-compose.yml` 删除 `auth-service:` 整个段落（110-126 行）。

- [ ] **Step 4: 删除 auth_service cmd 目录**

Run: `rm -rf cmd/auth_service`
确认没有其他引用（`grep -rn "auth_service" --include="*.go" .`，Dockerfile 构建参数无残留）。

- [ ] **Step 5: 构建验证**

Run: `go build ./... && go vet ./...`
Expected: 通过。`docker compose config` 无 auth-service。

- [ ] **Step 6: 提交**

```bash
git add cmd/user_service/main.go internal/grpc_client/client.go docker-compose.yml
git rm cmd/auth_service/main.go
git commit -m "refactor(auth): merge auth_service into user_service, drop separate auth container"
```

---

## Phase 2: 事件写 + Outbox

### Task 10: Outbox 基础设施（模型 + repo + 发布器 + 事件 DTO + 生产/消费接线）

**Files:**
- Create: `internal/model/outbox.go`
- Create: `internal/domain/store/outbox_store.go`
- Create: `internal/dao/mysql/outbox/outbox_store.go`
- Modify: `internal/dao/mysql/stores.go`（加 OutboxRepo）
- Modify: `internal/dao/mysql/init_mysql.go`（AutoMigrate 按服务收敛，Task 20 细做；本步先加 Outbox 表）
- Create: `internal/dto/event/domain_event.go`
- Create: `pkg/outbox/kafka.go`
- Create: `pkg/outbox/publisher.go`
- Create: `internal/service/message/domain_event_consumer.go`
- Create: `internal/service/message/session_event_handler.go`

- [ ] **Step 1: Outbox 模型**

`internal/model/outbox.go`：
```go
package model

import "time"

// Outbox 事务发件箱
// 与业务写在同一本地事务，发布器轮询发送后标记 published
type Outbox struct {
	Uuid      string    `gorm:"column:uuid;primaryKey;type:char(20);comment:事件ID"`
	EventType string    `gorm:"column:event_type;type:varchar(50);not null;index;comment:事件类型"`
	Payload   string    `gorm:"column:payload;type:text;not null;comment:事件JSON负载"`
	Status    int8      `gorm:"column:status;not null;default:0;comment:0=待发布 1=已发布"`
	RetryCount int32    `gorm:"column:retry_count;not null;default:0;comment:重试次数"`
	CreatedAt time.Time `gorm:"column:created_at;not null;comment:创建时间"`
	PublishedAt *time.Time `gorm:"column:published_at;comment:发布时间"`
}
```
若仓库使用软删除约定（`gorm.Model`），此处**不加** `gorm.Model`，避免软删字段干扰重发语义。

- [ ] **Step 2: OutboxStore 接口**

`internal/domain/store/outbox_store.go`：
```go
package store

import (
	"context"
	"kama_chat_server/internal/model"
)

type OutboxStore interface {
	Create(ctx context.Context, outbox *model.Outbox) error
	FindPending(ctx context.Context, limit int) ([]model.Outbox, error)
	MarkPublished(ctx context.Context, uuids []string) error
	IncrementRetry(ctx context.Context, uuid string) error
}
```

- [ ] **Step 3: GORM 实现**

`internal/dao/mysql/outbox/outbox_store.go`：
```go
package outbox

import (
	"context"
	"time"

	"gorm.io/gorm"

	"kama_chat_server/internal/model"
)

type outboxStore struct {
	db *gorm.DB
}

func NewOutboxStore(db *gorm.DB) *outboxStore {
	return &outboxStore{db: db}
}

func (r *outboxStore) Create(ctx context.Context, o *model.Outbox) error {
	return r.db.WithContext(ctx).Create(o).Error
}

func (r *outboxStore) FindPending(ctx context.Context, limit int) ([]model.Outbox, error) {
	if limit <= 0 {
		limit = 100
	}
	var list []model.Outbox
	err := r.db.WithContext(ctx).
		Where("status = ?", 0).
		Order("created_at ASC").
		Limit(limit).
		Find(&list).Error
	return list, err
}

func (r *outboxStore) MarkPublished(ctx context.Context, uuids []string) error {
	if len(uuids) == 0 {
		return nil
	}
	return r.db.WithContext(ctx).Model(&model.Outbox{}).
		Where("uuid IN ?", uuids).
		Updates(map[string]interface{}{"status": 1, "published_at": time.Now()}).Error
}

func (r *outboxStore) IncrementRetry(ctx context.Context, uuid string) error {
	return r.db.WithContext(ctx).Model(&model.Outbox{}).
		Where("uuid = ?", uuid).
		UpdateColumn("retry_count", gorm.Expr("retry_count + 1")).Error
}
```
确保实现 `store.OutboxStore`（`var _ store.OutboxStore = (*outboxStore)(nil)`）。

- [ ] **Step 4: Stores 增加 OutboxRepo**

`internal/dao/mysql/stores.go`：
```go
Outbox    store.OutboxStore
...
Outbox:    outbox.NewOutboxStore(db),
```
及访问器 `OutboxStore() store.OutboxStore`。补 import `"kama_chat_server/internal/dao/mysql/outbox"`。

- [ ] **Step 5: UnitOfWork 增加 OutboxStore()**

`internal/domain/store/transaction.go` 接口增加 `OutboxStore() OutboxStore`；`fakeUOW`（Task 2 测试）补该方法返回 nil。

- [ ] **Step 6: AutoMigrate 加 Outbox 表**

`internal/dao/mysql/init_mysql.go` 的 `AutoMigrate` 列表追加 `&model.Outbox{}`（暂全服务迁移；Task 20 收敛）。

- [ ] **Step 7: 事件 DTO 与类型常量**

`internal/dto/event/domain_event.go`：
```go
package event

const (
	EventGroupCreated   = "group_created"
	EventGroupJoined    = "group_joined"
	EventGroupDismissed = "group_dismissed"
	EventGroupUpdated   = "group_updated"
	EventUserUpdated    = "user_updated"
)

// GroupCreatedEvent 建群事件（relation → message）
type GroupCreatedEvent struct {
	GroupId     string `json:"group_id"`
	OwnerId     string `json:"owner_id"`
	GroupName   string `json:"group_name"`
	GroupAvatar string `json:"group_avatar"`
}

// GroupJoinedEvent 入群事件（relation → message）
type GroupJoinedEvent struct {
	GroupId     string `json:"group_id"`
	UserId      string `json:"user_id"`
	GroupName   string `json:"group_name"`
	GroupAvatar string `json:"group_avatar"`
}

// GroupDismissedEvent 解散群事件（relation → message）
type GroupDismissedEvent struct {
	GroupId string `json:"group_id"`
}

// GroupUpdatedEvent 群信息变更事件（relation → message）
type GroupUpdatedEvent struct {
	GroupId string `json:"group_id"`
	Name    *string `json:"name,omitempty"`
	Avatar  *string `json:"avatar,omitempty"`
}

// UserUpdatedEvent 用户资料变更事件（user → message）
type UserUpdatedEvent struct {
	UserId   string  `json:"user_id"`
	Nickname *string `json:"nickname,omitempty"`
	Avatar   *string `json:"avatar,omitempty"`
}
```

- [ ] **Step 8: Kafka 生产者封装**

`pkg/outbox/kafka.go`：
```go
package outbox

import (
	"context"

	"github.com/segmentio/kafka-go"

	"kama_chat_server/internal/config"
)

const DomainEventsTopic = "domain_events"

func NewProducer() *kafka.Writer {
	cfg := config.GetConfig().KafkaConfig
	return &kafka.Writer{
		Addr:                   kafka.TCP(cfg.HostPort),
		Topic:                  DomainEventsTopic,
		Balancer:               &kafka.Hash{},
		WriteTimeout:           cfg.Timeout * time.Second,
		RequiredAcks:           kafka.RequireNone,
		AllowAutoTopicCreation: true,
	}
}

func Publish(ctx context.Context, w *kafka.Writer, eventType string, eventUuid string, payload []byte) error {
	msg := kafka.Message{
		Key:   []byte(eventUuid),
		Value: payload,
		Headers: []kafka.Header{
			{Key: "event_type", Value: []byte(eventType)},
		},
	}
	return w.WriteMessages(ctx, msg)
}
```
（`time` 需 import。）

- [ ] **Step 9: 发布器（轮询 + 发 Kafka + 标记已发布）**

`pkg/outbox/publisher.go`：
```go
package outbox

import (
	"context"
	"time"

	"github.com/segmentio/kafka-go"
	"go.uber.org/zap"

	"kama_chat_server/internal/domain/store"
)

// Publisher 轮询 outbox 表，将待发布事件发送到 Kafka domain_events
type Publisher struct {
	outboxRepo store.OutboxStore
	producer   *kafka.Writer
	interval   time.Duration
	batchSize  int
}

func NewPublisher(repo store.OutboxStore, producer *kafka.Writer) *Publisher {
	return &Publisher{
		outboxRepo: repo,
		producer:   producer,
		interval:   1 * time.Second,
		batchSize:  100,
	}
}

func (p *Publisher) Start() {
	go func() {
		for {
			time.Sleep(p.interval)
			if err := p.Dispatch(context.Background()); err != nil {
				zap.L().Error("outbox dispatch error", zap.Error(err))
			}
		}
	}()
}

func (p *Publisher) Dispatch(ctx context.Context) error {
	events, err := p.outboxRepo.FindPending(ctx, p.batchSize)
	if err != nil {
		return err
	}
	if len(events) == 0 {
		return nil
	}
	var published []string
	for _, e := range events {
		if err := Publish(ctx, p.producer, e.EventType, e.Uuid, []byte(e.Payload)); err != nil {
			zap.L().Error("publish outbox event error", zap.String("uuid", e.Uuid), zap.String("type", e.EventType), zap.Error(err))
			_ = p.outboxRepo.IncrementRetry(ctx, e.Uuid)
			continue
		}
		published = append(published, e.Uuid)
	}
	if len(published) > 0 {
		return p.outboxRepo.MarkPublished(ctx, published)
	}
	return nil
}
```

- [ ] **Step 10: message_service 事件消费者**

`internal/service/message/domain_event_consumer.go`：
```go
package message

import (
	"context"
	"encoding/json"
	"fmt"
	"os"
	"time"

	"github.com/segmentio/kafka-go"
	"go.uber.org/zap"

	"kama_chat_server/internal/config"
	"kama_chat_server/internal/dto/event"
	"kama_chat_server/pkg/outbox"
)

// DomainEventConsumer 消费 domain_events topic，处理跨服务会话变更
type DomainEventConsumer struct {
	reader  *kafka.Reader
	handler *SessionEventHandler
	quit    chan os.Signal
}

func NewDomainEventConsumer(handler *SessionEventHandler) *DomainEventConsumer {
	kafkaConfig := config.GetConfig().KafkaConfig
	reader := kafka.NewReader(kafka.ReaderConfig{
		Brokers:        []string{kafkaConfig.HostPort},
		Topic:          outbox.DomainEventsTopic,
		CommitInterval: kafkaConfig.Timeout * time.Second,
		GroupID:        "message_domain_events",
		StartOffset:    kafka.LastOffset,
	})
	return &DomainEventConsumer{reader: reader, handler: handler, quit: make(chan os.Signal, 1)}
}

func (c *DomainEventConsumer) Start() {
	go func() {
		defer func() {
			if r := recover(); r != nil {
				zap.L().Error(fmt.Sprintf("domain event consumer panic: %v", r))
			}
		}()
		for {
			msg, err := c.reader.ReadMessage(context.Background())
			if err != nil {
				zap.L().Error("read domain event error", zap.Error(err))
				continue
			}
			eventType := ""
			for _, h := range msg.Headers {
				if h.Key == "event_type" {
					eventType = string(h.Value)
				}
			}
			if err := c.handler.Handle(context.Background(), eventType, msg.Value); err != nil {
				zap.L().Error("handle domain event error", zap.String("type", eventType), zap.Error(err))
			}
		}
	}()
}

func (c *DomainEventConsumer) Close() {
	if c.reader != nil {
		c.reader.Close()
	}
}
```

- [ ] **Step 11: 事件处理器（建/改/删 session）**

`internal/service/message/session_event_handler.go`：
```go
package message

import (
	"context"
	"encoding/json"
	"strings"

	"go.uber.org/zap"

	"kama_chat_server/internal/domain/store"
	"kama_chat_server/internal/dto/event"
	"kama_chat_server/internal/infrastructure/snowflake"
	"kama_chat_server/internal/model"
)

// SessionEventHandler 将跨服务领域事件转换为本地 session 表操作
type SessionEventHandler struct {
	sessionRepo store.SessionStore
}

func NewSessionEventHandler(sessionRepo store.SessionStore) *SessionEventHandler {
	return &SessionEventHandler{sessionRepo: sessionRepo}
}

func (h *SessionEventHandler) Handle(ctx context.Context, eventType string, payload []byte) error {
	switch eventType {
	case event.EventGroupCreated:
		var e event.GroupCreatedEvent
		if err := json.Unmarshal(payload, &e); err != nil {
			return err
		}
		return h.createGroupSession(ctx, e.GroupId, e.OwnerId, e.GroupName, e.GroupAvatar)
	case event.EventGroupJoined:
		var e event.GroupJoinedEvent
		if err := json.Unmarshal(payload, &e); err != nil {
			return err
		}
		return h.createGroupSession(ctx, e.GroupId, e.UserId, e.GroupName, e.GroupAvatar)
	case event.EventGroupDismissed:
		var e event.GroupDismissedEvent
		if err := json.Unmarshal(payload, &e); err != nil {
			return err
		}
		return h.sessionRepo.SoftDeleteByUsers(ctx, []string{e.GroupId})
	case event.EventGroupUpdated:
		var e event.GroupUpdatedEvent
		if err := json.Unmarshal(payload, &e); err != nil {
			return err
		}
		updates := make(map[string]interface{})
		if e.Name != nil {
			updates["receive_name"] = *e.Name
		}
		if e.Avatar != nil {
			updates["avatar"] = *e.Avatar
		}
		if len(updates) == 0 {
			return nil
		}
		return h.sessionRepo.UpdateByReceiveId(ctx, e.GroupId, updates)
	case event.EventUserUpdated:
		var e event.UserUpdatedEvent
		if err := json.Unmarshal(payload, &e); err != nil {
			return err
		}
		updates := make(map[string]interface{})
		if e.Nickname != nil {
			updates["receive_name"] = *e.Nickname
		}
		if e.Avatar != nil {
			updates["avatar"] = *e.Avatar
		}
		if len(updates) == 0 {
			return nil
		}
		return h.sessionRepo.UpdateByReceiveId(ctx, e.UserId, updates)
	}
	return nil
}

// createGroupSession 建群/入群时创建群会话（幂等：已存在则跳过）
func (h *SessionEventHandler) createGroupSession(ctx context.Context, groupId, userId, groupName, groupAvatar string) error {
	existing, err := h.sessionRepo.FindBySendIdAndReceiveId(ctx, userId, groupId)
	if err == nil && existing != nil {
		return nil // 已存在，幂等跳过
	}
	if strings.HasPrefix(groupId, "G") == false {
		return nil
	}
	sess := model.Session{
		Uuid:        "S" + snowflake.GenerateIDString(),
		SendId:      userId,
		ReceiveId:   groupId,
		ReceiveName: groupName,
		Avatar:      groupAvatar,
	}
	return h.sessionRepo.CreateSession(ctx, &sess)
}
```

- [ ] **Step 12: 发布器与消费者接线进 main**

- `cmd/relation_service/main.go`：初始化 outbox Publisher（`outbox.NewPublisher(repos.OutboxRepo, outbox.NewProducer()).Start()`）。
- `cmd/user_service/main.go`：同上（本阶段 W6 用）。
- `cmd/message_service/main.go`：初始化 `NewSessionEventHandler(repos.Session)` + `NewDomainEventConsumer(handler).Start()` + defer Close。
（先用占位——Task 11-16 逐一在写路径里真正写入 outbox 行。）

- [ ] **Step 13: 测试与构建**

先写发布器单测 `pkg/outbox/publisher_test.go`（用内存 fake OutboxStore 验证：轮询把 pending 标记 published、失败重试计数 +1）：
```go
package outbox

import (
	"context"
	"testing"

	"kama_chat_server/internal/model"
)

type fakeOutboxRepo struct {
	pending []model.Outbox
	published []string
	retries  map[string]int
}

func (f *fakeOutboxRepo) Create(ctx context.Context, o *model.Outbox) error { return nil }
func (f *fakeOutboxRepo) FindPending(ctx context.Context, limit int) ([]model.Outbox, error) {
	return f.pending, nil
}
func (f *fakeOutboxRepo) MarkPublished(ctx context.Context, uuids []string) error {
	f.published = append(f.published, uuids...)
	return nil
}
func (f *fakeOutboxRepo) IncrementRetry(ctx context.Context, uuid string) error {
	f.retries[uuid]++
	return nil
}

func TestDispatchMarksPublished(t *testing.T) {
	repo := &fakeOutboxRepo{
		pending: []model.Outbox{{Uuid: "E1", EventType: "group_created", Payload: "{}"}},
		retries: map[string]int{},
	}
	// 使用真实 producer 需要 kafka；此处用一个注入的 PublishFn 替代（见下）
}
```
为避免单测依赖真实 Kafka，给 `Publisher` 增加可注入发送函数：
```go
type PublishFunc func(ctx context.Context, eventType string, uuid string, payload []byte) error
```
`Dispatch` 内用 `p.publishFn`（默认为 `Publish`）。测试注入 fake publishFn 断言 published/retries。改完跑 `go test ./pkg/outbox/ -v` 与 `go build ./...`。

- [ ] **Step 14: 提交**

```bash
git add internal/model/outbox.go internal/domain/store/outbox_store.go internal/dao/mysql/outbox/outbox_store.go internal/dao/mysql/stores.go internal/dao/mysql/init_mysql.go internal/domain/store/transaction.go internal/dto/event/domain_event.go pkg/outbox internal/service/message/domain_event_consumer.go internal/service/message/session_event_handler.go cmd/relation_service/main.go cmd/user_service/main.go cmd/message_service/main.go
git commit -m "feat(outbox): add transactional outbox infra, event DTOs, publisher and message-side consumer"
```

---

### Task 11: W6 — UpdateUserInfo 改为"本地事务 + outbox(user_updated)"

**Files:**
- Modify: `internal/service/user/service.go`（236-251 行）
- Modify: `internal/dto/event/domain_event.go`（如需发送辅助，见下）

- [ ] **Step 1: 写失败测试**

`internal/service/user/service_test.go` 追加：
```go
func TestUpdateUserInfo_EmitsUserUpdatedEvent(t *testing.T) {
	svc := &UserService{uow: &fakeUOW{userRepo: &fakeUserRepo{users: map[string]model.UserInfo{
		"U1": {Uuid: "U1", Nickname: "old", Avatar: "old_av"},
	}}}}
	nick := "new"
	av := "new_av"
	err := svc.UpdateUserInfo(context.Background(), "U1", userreq.UpdateUserInfoRequest{
		Nickname: &nick, Avatar: &av,
	})
	if err != nil {
		t.Fatalf("unexpected error: %v", err)
	}
	// 断言 outbox 写入了 user_updated 事件（fakeUOW 需记录 OutboxRepo.Create 调用）
	if !svc.lastOutboxEventCalled("user_updated") {
		t.Error("expected user_updated outbox event")
	}
}
```
（`UserService` 需暴露测试钩子或让 fakeUOW.OutboxRepo 记录写入。实现时给 UserService 加字段 `outboxRepo store.OutboxStore`，构造注入；测试断言 `svc.outboxRepo.(*fakeOutbox).lastType == event.EventUserUpdated`。此步先写，预期编译失败/断言失败。）

- [ ] **Step 2: UserService 注入 outboxRepo 并改造 UpdateUserInfo**

`internal/service/user/service.go`：
- 结构体加 `outboxRepo store.OutboxStore`，构造 `NewUserService` 增加参数（检查当前调用点：user_service main、auth_service main、services.go；同步更新）。
- `UpdateUserInfo` 事务内第 2 步"更新 Session 冗余字段"替换为：在事务内写 outbox 行：
```go
// 2. 事务内写 outbox 事件，message_service 消费后更新 session 冗余字段
nick := updateReq.Nickname
av := updateReq.Avatar
if nick != nil || av != nil {
	payload, _ := json.Marshal(event.UserUpdatedEvent{UserId: userId, Nickname: nick, Avatar: av})
	o := model.Outbox{
		Uuid:      fmt.Sprintf("O%s", snowflake.GenerateIDString()),
		EventType: event.EventUserUpdated,
		Payload:   string(payload),
		Status:    0,
		CreatedAt: time.Now(),
	}
	if err := tx.OutboxStore().Create(ctx, &o); err != nil {
		return err
	}
}
```
删除 `tx.SessionStore().UpdateByReceiveId(...)` 调用。补 import：`"encoding/json"`、`"kama_chat_server/internal/dto/event"`、`"kama_chat_server/internal/model"`、`"kama_chat_server/internal/infrastructure/snowflake"`、`"time"`。
注意：`updateReq.Nickname`/`Avatar` 是指针，`sessionUpdates` 逻辑原本只在有变更时更新——保持"仅昵称/头像变更才发事件"。

- [ ] **Step 3: 更新所有 NewUserService 调用点**

grep `NewUserService(`：`cmd/user_service/main.go`、`cmd/auth_service/main.go`（Task 9 已删，若无）、`internal/service/services.go`（Task 21 删，先同步改）。传入 `repos.Outbox`。

- [ ] **Step 4: 测试通过 + 构建**

Run: `go test ./internal/service/user/ -v && go build ./...`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add internal/service/user/service.go internal/service/user/service_test.go cmd/user_service/main.go
git commit -m "feat(user): emit user_updated outbox event instead of cross-db session update"
```

---

### Task 12: W1 — CreateGroup 改为 outbox(group_created)

**Files:**
- Modify: `internal/service/group/service.go`（CreateGroup:40-88）

- [ ] **Step 1: GroupService 注入 outboxRepo**

`internal/service/group/service.go` 结构体加 `outboxRepo store.OutboxStore`，构造 `NewGroupService` 增加参数。更新调用点：`cmd/relation_service/main.go`、`internal/service/services.go`。

- [ ] **Step 2: CreateGroup 事务内移除建 session，改写 outbox**

`internal/service/group/service.go` CreateGroup 事务内：
- 删除 66-77 行"创建会话"段；
- 事务尾部（成员创建后）追加：
```go
payload, _ := json.Marshal(event.GroupCreatedEvent{
	GroupId:     group.Uuid,
	OwnerId:     ownerId,
	GroupName:   group.Name,
	GroupAvatar: group.Avatar,
})
o := model.Outbox{
	Uuid:      fmt.Sprintf("O%s", snowflake.GenerateIDString()),
	EventType: event.EventGroupCreated,
	Payload:   string(payload),
	Status:    0,
	CreatedAt: time.Now(),
}
if err := tx.OutboxStore().Create(ctx, &o); err != nil {
	return errorx.ErrServerBusy
}
```
补 import：`"encoding/json"`、`"kama_chat_server/internal/dto/event"`、`"kama_chat_server/internal/model"`、`"kama_chat_server/internal/infrastructure/snowflake"`、`"time"`。

- [ ] **Step 3: 构建验证 + 单测**

Run: `go build ./...`
追加单测 `internal/service/group/service_test.go`：用 fakeUOW（OutboxRepo 记录）断言 CreateGroup 写入 `group_created` outbox 且不再调用 SessionRepo.CreateSession。

- [ ] **Step 4: 提交**

```bash
git add internal/service/group/service.go internal/service/group/service_test.go cmd/relation_service/main.go
git commit -m "feat(group): emit group_created outbox event instead of direct session creation"
```

---

### Task 13: W2 — ApplyGroup(DIRECT) 改为 outbox(group_joined)

**Files:**
- Modify: `internal/service/apply/service.go`（ApplyGroup DIRECT 分支 215-272）

- [ ] **Step 1: ApplyService 注入 outboxRepo**

`internal/service/apply/service.go` 结构体加 `outboxRepo store.OutboxStore`，构造 `NewApplyService` 增加参数。更新调用点：`cmd/relation_service/main.go`、`internal/service/services.go`。

- [ ] **Step 2: DIRECT 分支移除建 session，改写 outbox**

`ApplyGroup` 事务内：
- 删除 239-250 行"创建群聊会话"段；
- 事务尾部（`IncrementMemberCount` 之后）追加：
```go
payload, _ := json.Marshal(event.GroupJoinedEvent{
	GroupId:     req.GroupId,
	UserId:      userId,
	GroupName:   group.Name,
	GroupAvatar: group.Avatar,
})
o := model.Outbox{
	Uuid:      fmt.Sprintf("O%s", snowflake.GenerateIDString()),
	EventType: event.EventGroupJoined,
	Payload:   string(payload),
	Status:    0,
	CreatedAt: time.Now(),
}
if err := tx.OutboxStore().Create(ctx, &o); err != nil {
	return errorx.ErrServerBusy
}
```
补 import（同 Task 12）。

- [ ] **Step 3: 构建验证**

Run: `go build ./... && go vet ./...`
Expected: 通过。

- [ ] **Step 4: 提交**

```bash
git add internal/service/apply/service.go cmd/relation_service/main.go
git commit -m "feat(apply): emit group_joined outbox event for DIRECT group join"
```

---

### Task 14: W3 — PassGroupApply 改为 outbox(group_joined)

**Files:**
- Modify: `internal/service/apply/service.go`（PassGroupApply 事务 604-656）

- [ ] **Step 1: PassGroupApply 事务内移除建 session，改写 outbox**

`PassGroupApply` 事务内：
- 删除 643-654 行"创建群聊会话"段；
- `IncrementMemberCount` 之后追加（`group` 变量来自事务内 `tx.GroupStore().FindByUuid`）：
```go
payload, _ := json.Marshal(event.GroupJoinedEvent{
	GroupId:     groupId,
	UserId:      applicantId,
	GroupName:   group.Name,
	GroupAvatar: group.Avatar,
})
o := model.Outbox{
	Uuid:      fmt.Sprintf("O%s", snowflake.GenerateIDString()),
	EventType: event.EventGroupJoined,
	Payload:   string(payload),
	Status:    0,
	CreatedAt: time.Now(),
}
if err := tx.OutboxStore().Create(ctx, &o); err != nil {
	return errorx.ErrServerBusy
}
```

- [ ] **Step 2: 构建验证**

Run: `go build ./... && go vet ./...`
Expected: 通过。

- [ ] **Step 3: 提交**

```bash
git add internal/service/apply/service.go
git commit -m "feat(apply): emit group_joined outbox event on pass group apply"
```

---

### Task 15: W4/W5 — DismissGroup 与 UpdateGroupInfo 改为 outbox

**Files:**
- Modify: `internal/service/group/service.go`（DismissGroup:256-320、UpdateGroupInfo:355-400）

- [ ] **Step 1: DismissGroup 移除 session 软删，改 outbox**

`DismissGroup` 事务内：
- 删除 280-285 行 `tx.SessionStore().SoftDeleteByUsers(ctx, []string{groupId})`；
- 事务尾部（`ApplyStore().SoftDeleteByUsers` 之后）追加：
```go
payload, _ := json.Marshal(event.GroupDismissedEvent{GroupId: groupId})
o := model.Outbox{
	Uuid:      fmt.Sprintf("O%s", snowflake.GenerateIDString()),
	EventType: event.EventGroupDismissed,
	Payload:   string(payload),
	Status:    0,
	CreatedAt: time.Now(),
}
if err := tx.OutboxStore().Create(ctx, &o); err != nil {
	return errorx.ErrServerBusy
}
```

- [ ] **Step 2: UpdateGroupInfo 移除 session 冗余更新，改 outbox**

`UpdateGroupInfo`：
- 删除 370-380 行 `g.uow.SessionStore().UpdateByReceiveId(...)`（不在事务内）；
- 用 `g.uow.WithTx` 包裹群更新 + outbox 写入（原 `GroupStore().Update` 保留），追加：
```go
payload, _ := json.Marshal(event.GroupUpdatedEvent{
	GroupId: req.Uuid,
	Name:    req.Name,
	Avatar:  req.Avatar,
})
o := model.Outbox{
	Uuid:      fmt.Sprintf("O%s", snowflake.GenerateIDString()),
	EventType: event.EventGroupUpdated,
	Payload:   string(payload),
	Status:    0,
	CreatedAt: time.Now(),
}
if err := tx.OutboxStore().Create(ctx, &o); err != nil {
	return errorx.ErrServerBusy
}
```
注意：`req.Name`/`req.Avatar` 为指针（proto optional），`GroupUpdatedEvent.Name` 也是 `*string`，直接透传。仅当 Name/Avatar 任一变更才发事件。

- [ ] **Step 3: 构建验证**

Run: `go build ./... && go vet ./...`
Expected: 通过。

- [ ] **Step 4: 提交**

```bash
git add internal/service/group/service.go
git commit -m "feat(group): emit group_dismissed/group_updated outbox events instead of session writes"
```

---

### Task 16: 端到端事件流回归（单库阶段验证）

**Files:**
- （无需代码改动，验证 Task 10-15 成果）

- [x] **Step 1: 启动全部服务**

Run:
```bash
docker-compose up --build -d
```
Expected: 6 个容器 healthy（chat-server / user-service / relation-service / message-service / mysql / redis / kafka / etcd —— auth-service 已移除）。

- [x] **Step 2: 运行端到端回归**

Run: `python3 test_all_apis.py`
Expected: 全部断言通过。重点场景：
1. 注册用户 A、B → 登录拿 token；
2. A 加 B 好友，B 通过 → A 会话列表出现 B（会话由 message_service 侧 OpenSession 创建，非 outbox）；
3. A 建群 → A 会话列表出现该群（**outbox group_created 触发**）；
4. B 申请入群（或 A 设置 DIRECT 直接拉 B 进群）→ B 会话列表出现该群（**outbox group_joined 触发**）；
5. A 修改昵称/头像 → B 的私聊会话冗余昵称更新（**outbox user_updated 触发**）；
6. A 修改群名 → 群会话冗余名更新（**outbox group_updated 触发**）；
7. A 解散群 → 成员群会话被软删（**outbox group_dismissed 触发**）。

- [x] **Step 3: 验证 outbox 落库与消费**

Run:
```bash
docker exec chat-mysql mysql -uroot -proot123456 -e "SELECT uuid,event_type,status FROM chat.outbox ORDER BY created_at DESC LIMIT 20;"
```
Expected: 存在 status=1 的记录（已发布）。再 `docker logs message-service 2>&1 | grep -i "domain event"` 应无持续报错。

- [x] **Step 4: 提交（若有调试修复）**

若有修复，逐个 commit。

---

## Phase 3: 逻辑隔离（物理拆库）

### Task 17: split_db.sql + split_migrate.sql

**Files:**
- Create: `migrations/split_db.sql`
- Create: `migrations/split_migrate.sql`

- [ ] **Step 1: 建 3 库 3 账号 + GRANT**

`migrations/split_db.sql`：
```sql
-- 拆分：user 库
CREATE DATABASE IF NOT EXISTS chat_user CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
-- 拆分：relation 库
CREATE DATABASE IF NOT EXISTS chat_relation CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
-- 拆分：message 库
CREATE DATABASE IF NOT EXISTS chat_message CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 服务账号
CREATE USER IF NOT EXISTS 'svc_user'@'%' IDENTIFIED BY 'svc_user_pwd';
CREATE USER IF NOT EXISTS 'svc_relation'@'%' IDENTIFIED BY 'svc_relation_pwd';
CREATE USER IF NOT EXISTS 'svc_message'@'%' IDENTIFIED BY 'svc_message_pwd';

-- 最小权限原则：各服务只能访问自己的库
GRANT SELECT, INSERT, UPDATE, DELETE, CREATE, ALTER, INDEX, DROP ON chat_user.* TO 'svc_user'@'%';
GRANT SELECT, INSERT, UPDATE, DELETE, CREATE, ALTER, INDEX, DROP ON chat_relation.* TO 'svc_relation'@'%';
GRANT SELECT, INSERT, UPDATE, DELETE, CREATE, ALTER, INDEX, DROP ON chat_message.* TO 'svc_message'@'%';

FLUSH PRIVILEGES;
```

- [ ] **Step 2: 数据迁移脚本**

`migrations/split_migrate.sql`（一次性，假设旧数据在 `chat` 库；服务启动 AutoMigrate 建好新表后执行）：
```sql
-- 先由各服务 AutoMigrate 创建新库表结构，再搬数据
USE chat_user;
INSERT IGNORE INTO user_info SELECT * FROM chat.user_info;
INSERT IGNORE INTO outbox SELECT * FROM chat.outbox;

USE chat_relation;
INSERT IGNORE INTO group_info SELECT * FROM chat.group_info;
INSERT IGNORE INTO group_member SELECT * FROM chat.group_member;
INSERT IGNORE INTO friendship SELECT * FROM chat.friendship;
INSERT IGNORE INTO apply SELECT * FROM chat.apply;
INSERT IGNORE INTO outbox SELECT * FROM chat.outbox;

USE chat_message;
INSERT IGNORE INTO session SELECT * FROM chat.session;
INSERT IGNORE INTO message SELECT * FROM chat.message;
INSERT IGNORE INTO outbox SELECT * FROM chat.outbox;
```
（`INSERT IGNORE` 处理 uuid 主键冲突；若列名/类型一致可直接搬。表名以实际 gorm 表名映射为准：`user_info/group_info/group_member/friendship/apply/session/message`。）

- [ ] **Step 3: docker-compose mysql 挂载初始化 SQL**

`docker-compose.yml` mysql volumes 增加：
```yaml
- ./migrations/split_db.sql:/docker-entrypoint-initdb.d/split_db.sql:ro
```
（首次初始化自动执行；已存在 volume 时需 `docker-compose down -v` 重建再跑，或手动 `mysql < split_db.sql`。）

- [ ] **Step 4: 提交**

```bash
git add migrations/split_db.sql migrations/split_migrate.sql docker-compose.yml
git commit -m "feat(deps): add split_db.sql/split_migrate.sql and mount init script"
```

---

### Task 18: config 增加 MYSQL_USER / DB_NAME 环境变量覆盖

**Files:**
- Modify: `internal/config/config.go`（overlayEnvVars:143-163）

- [ ] **Step 1: 写失败测试**

`internal/config/config_test.go` 追加：
```go
func TestLoadConfig_EnvDBOverlay(t *testing.T) {
	os.Setenv("MYSQL_USER", "svc_message")
	os.Setenv("DB_NAME", "chat_message")
	defer os.Unsetenv("MYSQL_USER")
	defer os.Unsetenv("DB_NAME")

	conf, err := LoadConfig()
	if err != nil {
		t.Fatalf("LoadConfig failed: %v", err)
	}
	if conf.MysqlConfig.User != "svc_message" {
		t.Errorf("expected MYSQL_USER overlay, got %q", conf.MysqlConfig.User)
	}
	if conf.MysqlConfig.DatabaseName != "chat_message" {
		t.Errorf("expected DB_NAME overlay, got %q", conf.MysqlConfig.DatabaseName)
	}
}
```

- [ ] **Step 2: overlayEnvVars 追加两个变量**

`internal/config/config.go` 的 `overlayEnvVars` 追加：
```go
if v := os.Getenv("MYSQL_USER"); v != "" {
	c.MysqlConfig.User = v
}
if v := os.Getenv("DB_NAME"); v != "" {
	c.MysqlConfig.DatabaseName = v
}
```

- [ ] **Step 3: 测试通过 + 构建**

Run: `go test ./internal/config/ -v && go build ./...`
Expected: PASS。

- [ ] **Step 4: 提交**

```bash
git add internal/config/config.go internal/config/config_test.go
git commit -m "feat(config): add MYSQL_USER/DB_NAME env overlays for per-service database"
```

---

### Task 19: AutoMigrate 按服务收敛

**Files:**
- Modify: `internal/dao/mysql/init_mysql.go`

- [ ] **Step 1: 增加按服务迁移模型集合**

`internal/dao/mysql/init_mysql.go` 增加：
```go
// ServiceModels 每个服务只迁移自己库中的表
var ServiceModels = map[string][]interface{}{
	"user": {
		&model.UserInfo{}, &model.Outbox{},
	},
	"relation": {
		&model.GroupInfo{}, &model.GroupMember{},
		&model.Friendship{}, &model.Apply{}, &model.Outbox{},
	},
	"message": {
		&model.Session{}, &model.Message{}, &model.Outbox{},
	},
}

// InitFor 初始化指定服务对应的库表迁移
func InitFor(service string) *Stores {
	conf := config.GetConfig()
	dsn := fmt.Sprintf("%s:%s@tcp(%s:%d)/%s?charset=utf8mb4&parseTime=True&loc=Local",
		conf.MysqlConfig.User, conf.MysqlConfig.Password,
		conf.MysqlConfig.Host, conf.MysqlConfig.Port, conf.MysqlConfig.DatabaseName)
	db, err := gorm.Open(mysqldriver.Open(dsn), &gorm.Config{})
	if err != nil {
		zap.L().Fatal(err.Error())
	}
	models, ok := ServiceModels[service]
	if !ok {
		zap.L().Fatal("unknown service", zap.String("service", service))
	}
	if err := db.AutoMigrate(models...); err != nil {
		zap.L().Fatal(err.Error())
	}
	return NewStores(db)
}
```

- [ ] **Step 2: 各服务 main 改用 InitFor**

- `cmd/user_service/main.go`：`repos := mysqlimpl.InitFor("user")`
- `cmd/relation_service/main.go`：`repos := mysqlimpl.InitFor("relation")`
- `cmd/message_service/main.go`：`repos := mysqlimpl.InitFor("message")`
（`chat-server` 网关不连库，不动。）

- [ ] **Step 3: 构建验证**

Run: `go build ./... && go vet ./...`
Expected: 通过。`Init()` 保留给测试/脚本用。

- [ ] **Step 4: 提交**

```bash
git add internal/dao/mysql/init_mysql.go cmd/user_service/main.go cmd/relation_service/main.go cmd/message_service/main.go
git commit -m "feat(mysql): migrate only service-owned tables per database"
```

---

### Task 20: 各服务配置环境变量 + docker-compose env 注入

**Files:**
- Modify: `docker-compose.yml`
- Modify: `cmd/user_service/main.go`、`cmd/relation_service/main.go`、`cmd/message_service/main.go`（如硬编码 DSN 处，无则只配 env）

- [ ] **Step 1: docker-compose 为各服务注入 DB_NAME / MYSQL_USER**

`docker-compose.yml` 各服务 `environment:` 增加：
- `user-service`：
```yaml
    environment:
      MYSQL_USER: svc_user
      DB_NAME: chat_user
```
- `relation-service`：
```yaml
    environment:
      MYSQL_USER: svc_relation
      DB_NAME: chat_relation
```
- `message-service`：
```yaml
    environment:
      MYSQL_USER: svc_message
      DB_NAME: chat_message
```
（保留 `MYSQL_HOST=mysql`、`MYSQL_PASSWORD` 沿用 config.toml 的 root 密码覆盖——但注意 config.toml user=root 会被 MYSQL_USER 覆盖为 svc_*。）

- [ ] **Step 2: 确认密码一致**

`config.toml` 的 `password = "root123456"` 是 root 密码；svc_* 账号密码在 split_db.sql 定义（`svc_user_pwd` 等）。各服务需 `MYSQL_PASSWORD` 覆盖为对应账号密码：
```yaml
    environment:
      MYSQL_PASSWORD: svc_user_pwd   # 或 svc_relation_pwd / svc_message_pwd
```
（`overlayEnvVars` 已支持 MYSQL_PASSWORD。）

- [ ] **Step 3: 本地开发 `.env` 说明**

不修改 `configs/config.toml`（保持默认单库 root/chat 便于本地单库回归），本地拆库运行时导出环境变量：
```bash
export MYSQL_USER=svc_user DB_NAME=chat_user MYSQL_PASSWORD=svc_user_pwd
```

- [ ] **Step 4: 提交**

```bash
git add docker-compose.yml
git commit -m "feat(deps): inject per-service DB credentials via env in docker-compose"
```

---

### Task 21: 清理死代码与收尾构造

**Files:**
- Delete: `internal/service/services.go`
- Modify: `internal/service/session/service.go`（构造签名：去掉 userRepo/groupRepo/groupMemberRepo/friendshipRepo 依赖）
- Modify: `internal/service/message/service.go`（去掉 friendshipRepo 依赖）
- Modify: `internal/service/message/kafka_processor.go`（去掉 userRepo/friendshipRepo/groupMemberRepo 依赖）
- Modify: `cmd/message_service/main.go`（按新签名构造）
- Modify: `internal/dao/mysql/init_mysql.go`（`Init()` 删除，防误用）

- [x] **Step 1: 删除 services.go**

Run: `rm internal/service/services.go`
确认 `internal/service` 包剩余文件不受影响（该包只有此文件？——`ls internal/service/`，若只有 services.go 则包整体删除）。

- [x] **Step 2: session 服务精简依赖**

`internal/service/session/service.go`：构造签名改为 `NewSessionService(sessionRepo, messageRepo, cacheService)`；结构体删除 `userRepo/groupRepo/groupMemberRepo/friendshipRepo` 字段（Task 5 已改 gRPC，不再使用）。更新 `cmd/message_service/main.go` 调用。

- [x] **Step 3: message 服务精简依赖**

`internal/service/message/service.go`：构造改为 `NewMessageService(messageRepo, sessionRepo, cacheService, pushRecallNotify)`，删除 `friendshipRepo`。`kafka_processor.go` 构造改为 `NewKafkaProcessor(messageRepo, sessionRepo, cacheService)`，删除 `userRepo/friendshipRepo/groupMemberRepo` 与 `cacheHelper`（Task 7 已改 gRPC）。同步更新 `cmd/message_service/main.go` 与 `services.go`（已删，无）。

- [x] **Step 4: 删除 init_mysql.Init()**

`internal/dao/mysql/init_mysql.go`：删除 `Init()`（保留 `InitFor`），防意外回退到全表迁移。

- [x] **Step 5: 构建验证**

Run: `go build ./... && go vet ./...`
Expected: 通过。

- [x] **Step 6: 提交**

```bash
git rm internal/service/services.go
git add internal/service/session/service.go internal/service/message/service.go internal/service/message/kafka_processor.go cmd/message_service/main.go internal/dao/mysql/init_mysql.go
git commit -m "refactor: drop dead code and per-service repo dependencies after gRPC/event conversion"
```

---

### Task 22: 拆库验收（端到端 + 数据迁移）

**Files:**
- （验证任务）

- [x] **Step 1: 重建环境**

```bash
docker-compose down -v
docker-compose up --build -d
```
Expected: mysql 初始化执行 `split_db.sql`，8 个服务全部健康（auth-service 已移除）。

- [x] **Step 2: 执行数据迁移**

```bash
docker exec -i chat-mysql mysql -uroot -proot123456 < migrations/split_migrate.sql
```

- [x] **Step 3: 验证账号权限隔离**

```bash
docker exec chat-mysql mysql -usvc_relation -psvc_relation_pwd -e "USE chat_user; SHOW TABLES;" 2>&1
```
Expected: `ERROR 1142`（无权限）——证明 relation 账号访问不了 user 库。

- [x] **Step 4: 端到端回归**

Run: `python3 test_all_apis.py`
Expected: 全绿。再跑一遍 Task 16 Step 3 的 outbox 检查确认事件消费正常。

- [x] **Step 5: 提交（修复项）**

有失败则走 systematic-debugging 修复后提交。

---

### Task 23: 性能与链路验证

**Files:**
- （验证任务，按用户规范）

- [x] **Step 1: 压测**

对登录/发消息接口用 hey 压测，输出规范要求的完整指标：
```bash
hey -z 30s -c 50 http://localhost:8081/user/login
```
Expected 输出：RPS、平均延迟、P50/P99 延迟分布、总耗时、状态码分布。

- [x] **Step 2: Tempo trace 链路**

开启 `otelConfig.enabled=true`（config.toml），验证 OTLP exporter 到 Tempo，抓取一次登录 + 一次发消息的完整 trace（chat_server → gRPC → message_service → Kafka）。

- [x] **Step 3: 记录并汇报**

将 hey 结果与 trace 链接写入交付说明（不允许口头声称压测通过而无数据）。

- [x] **Step 4: 提交**

```bash
git add .  # 仅本次改动
git commit -m "docs: db-split verification results (load test + trace)"
```

---

#### Task 23 交付记录

**压测（hey，2026-08-03）**

| 接口 | QPS | 平均延迟 | P99 | 说明 |
|---|---|---|---|---|
| POST /auth/login | 88.97 RPS | 99.7ms | - | 含 bcrypt 校验，受单核 CPU 约束 |
| GET /sessions/direct | 1991 RPS | - | 162ms | 会话列表（Redis 缓存命中） |
| GET /messages | 12872 RPS | - | 10.3ms | 消息列表 |

**Tempo trace 链路（OTLP gRPC → tempo:4317）**

| 场景 | TraceID | 链路 |
|---|---|---|
| 登录 | `c33ad10306de7935817296f37caecadb` | `chat-server POST /auth/login` (HTTP server span) → `/auth.AuthService/Login` (gRPC client span, chat-server) → `/auth.AuthService/Login` (gRPC server span, user_service) |
| 发消息 | `6660d30f3eacdaa1eb79937e440854b1` | `chat-server kafka.publish` (producer span) → Kafka `chat_upstream` (traceparent 注入 headers) → `message_service kafka.consume` (consumer span，父 span = kafka.publish) |

**改动文件（本任务）：**
- 新增 `pkg/otel/grpc_propagation.go`：gRPC client/server unary 拦截器，基于已有 `go.opentelemetry.io/otel` 实现 trace context 传播（未引入新依赖，go.mod/go.sum 保持 WIP）
- `internal/grpc_client/client.go`：client 拦截器链（auth → trace）
- `cmd/{user,relation,message}_service/main.go`：InitTracer（按 `otelConfig.enabled` 门控，服务名固定）+ server 拦截器链
- `internal/service/chat/kafka_broker.go`：`kafka.publish` producer span
- `internal/service/message/kafka_processor.go`：`kafka.consume` consumer span（修复 `defer span.End()` 在 for 循环中永不执行的 bug）
- `docker-compose.yml` + `configs/tempo.yaml`：新增 Tempo 2.10.4 服务（本地镜像，端口 4317/3200）
- `configs/config.toml`：`otelConfig.enabled=true`、endpoint=`tempo:4317`

---

## Self-Review

**Spec 覆盖：**
- 步骤1 逻辑隔离 → Task 17-20（3库3账号 GRANT、AutoMigrate 收敛、config env、docker-compose）
- 步骤2 gRPC 读 → Task 1-8（BatchGetPublicUserInfo、CheckFriendship/CheckGroupMember、R1/R1b/R2）、Task 9（auth 并入=R7）
- 步骤3 事件写 + Outbox → Task 10-16（模型/发布器/消费者 + W1-W6 + 端到端回归）
- 幂等（SETNX + 存在性跳过）→ Task 10 处理器 + 文档说明
- spec 修正项（PassFriendApply/DeleteFriend/KickUser 不建 session、R3/R4/R5 无需改造、R1b 需新校验方法）→ 已在 Task 5/6/7/8 落实，W 清单按真实代码

**占位符检查：** 各 Step 均含具体代码或明确验证命令；两处"先 sed 查看再替换"（Task 8 Step 4、Task 11 测试钩子）给出了替换目标与方法，不是占位。

**类型一致性：**
- 事件类型常量在 Task 10 定义、Task 11-15 使用，命名一致（group_created/group_joined/group_dismissed/group_updated/user_updated）。
- `grpc_client` 封装函数名统一：GetUserStatus / BatchGetPublicUserInfo / CheckFriendshipStatus / CheckGroupMember / ListGroupMemberIds / GetGroupDetail / GetUserNicknameAvatar。
- CheckFriendship 对外契约（0=非好友 1=正常 2=拉黑对方 3=被拉黑）与内部 `friendship_status` 枚举（NORMAL=0/BE_BLACK=1/BLACK=2）的映射在 Task 3 Step 2 已定义，Task 5/6/7 的判定（status==1、==2、==3）均基于该契约，一致。
- `GetGroupMemberList`（需成员身份 + JOIN user 表 + pageSize≤100）**不可**用于 Task 7 群发，改用 Task 1/3 新增的 `ListGroupMemberIds`（直读 group_member.UserUuid）。
- OutboxRepo 接口方法：Create / FindPending / MarkPublished / IncrementRetry，Task 10 定义、Task 11-15 调用一致。
- `InitFor(service)` 与 `ServiceModels` 在 Task 19 定义、Task 19/21 使用。
