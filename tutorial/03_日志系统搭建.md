# 03. 日志系统搭建

> 本教程将使用 Zap + Lumberjack 构建一个生产级的日志系统，支持日志分级、文件轮转和结构化输出。

---

## 📌 学习目标

- 理解 Zap 日志库的优势
- 实现日志文件轮转
- 配置不同级别的日志输出
- 集成 Gin 请求日志中间件

---

## 1. 为什么选择 Zap

| 特性 | Zap | 标准库 log |
|-----|-----|-----------|
| 性能 | 极高（零分配） | 一般 |
| 结构化日志 | ✅ 原生支持 | ❌ |
| 日志级别 | ✅ Debug/Info/Warn/Error | ❌ |
| 自定义输出 | ✅ 多种 Encoder | 有限 |

**Zap** 是 Uber 开源的高性能日志库，特别适合高并发场景。

---

## 2. 安装依赖

```bash
go get go.uber.org/zap
go get gopkg.in/natefinch/lumberjack.v2
```

- **Zap**：高性能结构化日志
- **Lumberjack**：日志文件轮转（按大小/时间切割）

> **注意**：使用 `gopkg.in/natefinch/lumberjack.v2` 而不是 `github.com/natefinch/lumberjack`

---

## 3. 实现日志模块

### 3.1 代码结构

日志模块已拆分为多个文件，职责分离：

```
internal/infrastructure/logger/
├── logger.go   # Init 初始化 + 核心逻辑
├── writer.go   # getLogWriter 日志写入器
├── encoder.go  # getJSONEncoder / getConsoleEncoder 编码器
├── gin.go      # GinLogger / GinRecovery 中间件
└── utils.go   # isBrokenPipeError 辅助函数
```

### 3.2 internal/infrastructure/logger/logger.go

```go
package logger

import (
	"fmt"
	"os"

	"kama_chat_server/internal/config"

	"github.com/gin-gonic/gin"
	"go.uber.org/zap"
	"go.uber.org/zap/zapcore"
)

// Init 初始化日志系统
func Init(cfg *config.LogConfig, mode string) error {
	if cfg == nil {
		return fmt.Errorf("logger.Init received nil config")
	}

	// 设置默认值
	setDefaultIfEmpty(cfg)

	// 获取日志写入器和编码器
	writeSyncer := getLogWriter(cfg.FileName, cfg.MaxSize, cfg.MaxBackups, cfg.MaxAge)
	encoder := getJSONEncoder()

	var level zapcore.Level
	if err := level.UnmarshalText([]byte(cfg.Level)); err != nil {
		return err
	}

	var core zapcore.Core
	if mode == "dev" || mode == gin.DebugMode {
		// 开发模式：同时输出到控制台和文件
		core = newDevCore(encoder, writeSyncer, level)
	} else {
		// 生产模式：只输出到文件（JSON 格式）
		core = zapcore.NewCore(encoder, writeSyncer, level)
	}

	// 创建并替换全局 Logger
	lg := zap.New(core, zap.AddCaller())
	zap.ReplaceGlobals(lg)

	return nil
}

// setDefaultIfEmpty 设置配置默认值
func setDefaultIfEmpty(cfg *config.LogConfig) {
	if cfg.FileName == "" {
		cfg.FileName = cfg.LogPath + "/app.log"
	}
	if cfg.MaxSize == 0 {
		cfg.MaxSize = 100
	}
	if cfg.MaxBackups == 0 {
		cfg.MaxBackups = 5
	}
	if cfg.MaxAge == 0 {
		cfg.MaxAge = 30
	}
	if cfg.Level == "" {
		cfg.Level = "info"
	}
}

// newDevCore 创建开发模式的 Core
func newDevCore(encoder zapcore.Encoder, writeSyncer zapcore.WriteSyncer, level zapcore.Level) zapcore.Core {
	consoleEncoder := getConsoleEncoder()
	fileCore := zapcore.NewCore(encoder, writeSyncer, level)
	consoleCore := zapcore.NewCore(consoleEncoder, zapcore.Lock(os.Stdout), zapcore.DebugLevel)
	return zapcore.NewTee(fileCore, consoleCore)
}
```

### 3.3 writer.go - 日志写入器

```go
package logger

import (
	"gopkg.in/natefinch/lumberjack.v2"
	"go.uber.org/zap/zapcore"
)

// getLogWriter 获取日志写入器（支持日志轮转）
func getLogWriter(filename string, maxSize int, maxBackups int, maxAge int) zapcore.WriteSyncer {
	lumberjackLogger := &lumberjack.Logger{
		Filename:   filename,
		MaxSize:    maxSize,
		MaxBackups: maxBackups,
		MaxAge:     maxAge,
	}
	return zapcore.AddSync(lumberjackLogger)
}
```

### 3.4 encoder.go - 编码器

```go
package logger

import (
	"go.uber.org/zap/zapcore"
)

// getJSONEncoder 获取 JSON 编码器
// JSON 格式输出，适合机器解析和日志收集系统（如 ELK）
func getJSONEncoder() zapcore.Encoder {
	encoderConfig := zapcore.EncoderConfig{
		TimeKey:        "time",
		LevelKey:       "level",
		NameKey:        "logger",
		CallerKey:      "caller",
		MessageKey:     "msg",
		StacktraceKey:  "stacktrace",
		LineEnding:     zapcore.DefaultLineEnding,
		EncodeLevel:    zapcore.CapitalLevelEncoder,
		EncodeTime:     zapcore.ISO8601TimeEncoder,
		EncodeDuration: zapcore.SecondsDurationEncoder,
		EncodeCaller:   zapcore.ShortCallerEncoder,
	}
	return zapcore.NewJSONEncoder(encoderConfig)
}

// getConsoleEncoder 获取控制台编码器
// Console 格式输出，更易读，适合开发调试
func getConsoleEncoder() zapcore.Encoder {
	encoderConfig := zapcore.EncoderConfig{
		TimeKey:       "time",
		LevelKey:      "level",
		NameKey:       "logger",
		CallerKey:     "caller",
		MessageKey:    "msg",
		StacktraceKey: "stacktrace",
		LineEnding:    zapcore.DefaultLineEnding,
		EncodeLevel:   zapcore.CapitalColorLevelEncoder,
		EncodeTime:    zapcore.ISO8601TimeEncoder,
		EncodeDuration: zapcore.StringDurationEncoder,
		EncodeCaller:  zapcore.ShortCallerEncoder,
	}
	return zapcore.NewConsoleEncoder(encoderConfig)
}
```

---

## 4. Gin 请求日志中间件

项目实现了 Gin 专用的日志中间件，用于记录 HTTP 请求。

### 4.1 gin.go

```go
package logger

import (
	"net/http"
	"net/http/httputil"
	"time"

	"github.com/gin-gonic/gin"
	"go.uber.org/zap"
)

// GinLogger 记录 HTTP 请求日志
func GinLogger() gin.HandlerFunc {
	return func(c *gin.Context) {
		start := time.Now()
		c.Next()
		cost := time.Since(start)

		zap.L().Info("http request",
			zap.Int("status", c.Writer.Status()),
			zap.String("method", c.Request.Method),
			zap.String("path", c.Request.URL.Path),
			zap.String("query", c.Request.URL.RawQuery),
			zap.String("clientIP", c.ClientIP()),
			zap.String("user-agent", c.Request.UserAgent()),
			zap.Duration("cost", cost),
			zap.String("errors", c.Errors.ByType(gin.ErrorTypePrivate).String()),
		)
	}
}

// GinRecovery 捕获 panic 并恢复
func GinRecovery(stack bool) gin.HandlerFunc {
	return func(c *gin.Context) {
		defer func() {
			if rec := recover(); rec != nil {
				var brokenPipe bool
				if err, ok := rec.(error); ok {
					brokenPipe = isBrokenPipeError(err)
				}

				httpRequest, _ := httputil.DumpRequest(c.Request, false)
				fields := []zap.Field{
					zap.Any("error", rec),
					zap.String("request", string(httpRequest)),
				}

				if brokenPipe {
					zap.L().Error("broken pipe",
						append(fields, zap.String("path", c.Request.URL.Path))...,
					)
					c.Error(rec.(error))
					c.Abort()
					return
				}

				if stack {
					fields = append(fields, zap.String("stack", getStackTrace()))
				}
				zap.L().Error("[Recovery from panic]", fields...)
				c.AbortWithStatus(http.StatusInternalServerError)
			}
		}()
		c.Next()
	}
}
```

### 4.2 utils.go

```go
package logger

import (
	"errors"
	"net"
	"os"
	"runtime/debug"
	"strings"
)

// isBrokenPipeError 检查错误链中是否包含 broken pipe
func isBrokenPipeError(err error) bool {
	if err == nil {
		return false
	}

	var opErr *net.OpError
	if errors.As(err, &opErr) {
		var syscallErr *os.SyscallError
		if errors.As(opErr.Err, &syscallErr) {
			msg := strings.ToLower(syscallErr.Error())
			return strings.Contains(msg, "broken pipe") ||
				strings.Contains(msg, "connection reset by peer")
		}
	}

	msg := strings.ToLower(err.Error())
	return strings.Contains(msg, "broken pipe") ||
		strings.Contains(msg, "connection reset by peer")
}

// getStackTrace 获取堆栈信息
func getStackTrace() string {
	return string(debug.Stack())
}
```

---

## 5. 核心概念详解

### 5.1 Lumberjack 日志轮转

```go
lumberjackLogger := &lumberjack.Logger{
    Filename:   "logs/app.log",
    MaxSize:    100,  // 单个文件最大 100MB
    MaxBackups: 5,    // 保留 5 个旧文件
    MaxAge:     30,   // 保留 30 天
    Compress:   true, // 压缩旧文件为 .gz
}
```

**工作流程**：
1. 日志写入 `app.log`
2. 当文件达到 100MB，重命名为 `app-2024-01-01T10-00-00.log`
3. 创建新的 `app.log` 继续写入
4. 超过 5 个备份文件时，删除最旧的
5. 超过 30 天的文件也会被删除

### 5.2 Zap Core 架构

```
┌─────────────┐
│   Logger    │
└──────┬──────┘
       │
┌──────▼──────┐
│     Tee     │  <- 多路输出
├─────┬───────┤
│     │       │
▼     ▼       ▼
File  Console  ...
```

`zapcore.NewTee` 允许将日志同时输出到多个目标。

### 5.3 日志级别

```
Debug < Info < Warn < Error < DPanic < Panic < Fatal
```

设置级别为 `Info` 时，`Debug` 日志不会输出。

---

## 6. 使用方法

### 6.1 基本使用

```go
import "go.uber.org/zap"

// 使用全局 Logger
zap.L().Info("User logged in", 
    zap.String("userId", "U123456"),
    zap.String("ip", "192.168.1.1"),
)

zap.L().Error("Failed to save message",
    zap.Error(err),
    zap.String("messageId", "M789"),
)
```

### 6.2 输出示例

**控制台输出（开发模式）**：
```
2026-02-25T08:10:30.123+0800    INFO    logger/gin.go:21    http request    {"status": 200, "method": "GET", "path": "/api/user/info", "clientIP": "127.0.0.1", "cost": "15ms"}
```

**日志文件（JSON 格式，生产模式）**：
```json
{"level":"INFO","time":"2026-02-25T08:10:30.123Z","caller":"logger/gin.go:21","msg":"http request","status":200,"method":"GET","path":"/api/user/info","clientIP":"127.0.0.1","cost":0.015}
```

---

## 7. 更新 main.go

```go
package main

import (
	"fmt"
	"log"

	"kama_chat_server/internal/config"
	"kama_chat_server/internal/infrastructure/logger"
)

func main() {
	fmt.Println("KamaChat Server Starting...")

	// 1. 加载配置
	cfg := config.GetConfig()

	// 2. 初始化日志
	if err := logger.Init(&cfg.LogConfig, "dev"); err != nil {
		log.Fatalf("init logger failed: %v", err)
	}

	logger.Info("配置加载成功",
		logger.String("app", cfg.AppName),
		logger.Int("port", cfg.Port),
	)

	// TODO: 后续步骤
	// 3. 初始化数据库
	// 4. 初始化 Redis
	// 5. 启动服务
}
```

---

## 8. 在 Gin 中集成中间件

```go
// internal/https_server/https_server.go
import "kama_chat_server/internal/infrastructure/logger"

func Init() *gin.Engine {
	engine := gin.New()
	
	// 使用自定义日志中间件替代 gin.Logger()
	engine.Use(logger.GinLogger())
	
	// 使用自定义恢复中间件（带堆栈信息）
	engine.Use(logger.GinRecovery(true))
	
	return engine
}
```

---

## 9. 最佳实践

### 9.1 使用结构化字段

```go
// ✅ 推荐：结构化字段
zap.L().Info("Request processed",
    zap.String("method", "POST"),
    zap.String("path", "/api/user"),
    zap.Int("status", 200),
    zap.Duration("latency", time.Since(start)),
)

// ❌ 不推荐：字符串拼接
zap.L().Info(fmt.Sprintf("Request %s %s processed in %v", method, path, latency))
```

### 9.2 错误日志包含堆栈

```go
if err != nil {
    zap.L().Error("Database query failed",
        zap.Error(err),  // 自动记录错误信息
        zap.String("query", sql),
    )
}
```

### 9.3 敏感信息脱敏

```go
// 不要记录密码等敏感信息
zap.L().Info("User registered",
    zap.String("telephone", "138****8000"),  // 脱敏
)
```

---

## ✅ 本节完成

你已经完成了：
- [x] Zap + Lumberjack 日志系统
- [x] 日志文件轮转配置
- [x] 多路输出（文件 + 控制台）
- [x] 日志级别控制
- [x] Gin 请求日志中间件
- [x] Panic 恢复中间件

---

## 📚 下一步

继续学习 [04_数据库连接.md](04_数据库连接.md)，实现 GORM + MySQL 数据库集成。
