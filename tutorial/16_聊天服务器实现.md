# 16. 聊天服务器实现

> 本章以当前代码为准，解释聊天子系统如何通过 `ChatServer` 聚合 Kafka 消息消费者，以及 `MsgConsumer` 如何管理在线连接与消息消费循环。

---

## 📌 学习目标

- 理解 `ChatServer` 的职责：初始化 Kafka 消息消费者
- 理解 `*MsgConsumer` 对 WebSocket 网关的解耦作用
- 掌握 Kafka 模式 `MsgConsumer` 的并发模型
- 理解依赖注入：Repository/CacheService 如何传入 MsgConsumer

---

## 1. 代码位置（以当前实现为准）

- 服务器聚合：`internal/service/chat/server.go`
- Kafka 消息消费者：`internal/service/chat/kafka_broker.go`
- Kafka 客户端封装：`internal/service/chat/kafka_client.go`

> 注意：本项目**只支持 Kafka 模式**。

---

## 2. MsgConsumer：Kafka 消息消费者

WebSocket 网关直接依赖 `*MsgConsumer`，负责消息发布与消费。

```go
// internal/service/chat/kafka_broker.go

// MsgConsumer 基于 Kafka 的消息消费者
type MsgConsumer struct {
    Clients      sync.Map              // 在线客户端映射表
    Login        chan *UserConn        // 客户端登录通道
    Logout       chan *UserConn        // 客户端登出通道
    kafkaClient  *KafkaClient          // Kafka 客户端
    messageRepo  mysql.MessageRepository
    // ... 其他依赖
}

// Publish 发布消息到 Kafka
func (k *MsgConsumer) Publish(ctx context.Context, msg []byte) error {
    return k.kafkaClient.SendMessage(ctx, []byte("0"), msg)
}
    Close()
    GetMessageRepo() mysql.MessageRepository
}
```

**接口方法说明：**

| 方法 | 说明 |
|------|------|
| Publish | 发布消息到 Kafka |
| RegisterClient | 注册 WebSocket 客户端连接 |
| UnregisterClient | 注销客户端连接 |
| GetClient | 获取指定用户的在线连接 |
| KickClient | 单点登录互踢，推送下线通知 |
| PushRecallNotify | 本地直推撤回通知（非 Kafka） |
| Start | 启动消息消费循环 |
| Close | 关闭代理资源 |
| GetMessageRepo | 获取消息仓库，供 ws_gateway 更新消息状态 |

---

## 3. ChatServer：聚合与依赖注入

`ChatServer` 的职责是：

1. 接收配置与依赖（MessageRepo、FriendshipRepo、GroupMemberRepo、SessionRepo、CacheService、UserRepo）
2. 初始化 Kafka 客户端和消费者
3. 提供统一的 `Run()/Shutdown()` 生命周期

```go
// internal/service/chat/server.go

type ChatServer struct {
    // Broker 消息代理接口（MsgConsumer 实现）
    Broker *MsgConsumer
    // KafkaClient Kafka 客户端
    KafkaClient *KafkaClient
    // messageRepo 消息 Repository
    messageRepo mysql.MessageRepository
    // friendshipRepo 好友关系 Repository（用于消息权限校验）
    friendshipRepo mysql.FriendshipRepository
    // groupMemberRepo 群成员 Repository
    groupMemberRepo mysql.GroupMemberRepository
    // sessionRepo 会话 Repository（用于更新最后消息）
    sessionRepo mysql.SessionRepository
    // cacheService 缓存服务
    cacheService myredis.AsyncCacheService
    // userRepo 用户 Repository（用于检查用户状态）
    userRepo mysql.UserRepository
}

type ChatServerConfig struct {
    MessageRepo     mysql.MessageRepository
    FriendshipRepo  mysql.FriendshipRepository
    GroupMemberRepo mysql.GroupMemberRepository
    SessionRepo     mysql.SessionRepository
    CacheService    myredis.AsyncCacheService
    UserRepo        mysql.UserRepository
}

func NewChatServer(chatServerCfg ChatServerConfig) *ChatServer {
    cs := &ChatServer{
        messageRepo:     chatServerCfg.MessageRepo,
        friendshipRepo:  chatServerCfg.FriendshipRepo,
        groupMemberRepo: chatServerCfg.GroupMemberRepo,
        sessionRepo:     chatServerCfg.SessionRepo,
        cacheService:    chatServerCfg.CacheService,
        userRepo:        chatServerCfg.UserRepo,
    }

    // 初始化 Kafka 客户端和消费者
    cs.KafkaClient = NewKafkaClient()
    // 传递所有依赖用于消息发送权限校验
    cs.Broker = NewMsgConsumer(
        cs.KafkaClient,
        cs.messageRepo,
        cs.friendshipRepo,
        cs.groupMemberRepo,
        cs.sessionRepo,
        cs.cacheService,
        cs.userRepo,
    )

    return cs
}
```

**依赖说明：**

| 依赖 | 用途 |
|------|------|
| messageRepo | 消息 CRUD |
| friendshipRepo | 消息权限校验（检查是否为好友） |
| groupMemberRepo | 群成员查询 |
| sessionRepo | 更新会话最后消息 |
| cacheService | 异步缓存更新 |
| userRepo | 检查用户状态（是否被禁用） |

---

## 4. Kafka 模式：MsgConsumer 结构

Kafka 模式 Broker 结构体为 `MsgConsumer`：

```go
// internal/service/chat/kafka_broker.go

type MsgConsumer struct {
    Clients  sync.Map  // 在线用户表：userId -> *UserConn
    Producer *kafka.Writer
    Reader   *kafka.Reader

    // 依赖注入
    messageRepo     mysql.MessageRepository
    friendshipRepo  mysql.FriendshipRepository
    groupMemberRepo mysql.GroupMemberRepository
    sessionRepo     mysql.SessionRepository
    cacheService    myredis.AsyncCacheService
    userRepo        mysql.UserRepository
}
```

**设计要点：**

- 在线表使用 `sync.Map`，读写并发安全
- 通过 Kafka Producer/Reader 实现消息发布和消费
- 所有依赖通过构造函数注入，遵循依赖倒置原则

---

## 5. 消息消费循环：Start()

`Start()` 是 Kafka 模式的核心事件循环：

```go
// internal/service/chat/kafka_broker.go

func (mc *MsgConsumer) Start() {
    for {
        msg, err := mc.Reader.ReadMessage(context.Background())
        if err != nil {
            zap.L().Error("kafka read error", zap.Error(err))
            break
        }

        var req request.ChatMessageRequest
        if err := json.Unmarshal(msg.Value, &req); err != nil {
            continue
        }

        // 根据消息类型分发处理
        switch req.Type {
        case message_type.Text:
            mc.handleTextMessage(req)
        case message_type.Voice:
            mc.handleFileMessage(req)
        case message_type.File:
            mc.handleFileMessage(req)
        case message_type.AudioOrVideo:
            mc.handleAVMessage(req)
        case message_type.Recall:
            mc.handleRecallMessage(req)
        }
    }
}
```

---

## 6. Broker 接口方法实现

Kafka 消息消费者 `*MsgConsumer`：

```go
// Publish 发布消息到 Kafka
func (mc *MsgConsumer) Publish(ctx context.Context, msg []byte) error {
    return mc.Producer.WriteMessages(ctx, kafka.Message{
        Value: msg,
    })
}

// RegisterClient 注册客户端
func (mc *MsgConsumer) RegisterClient(client *UserConn) {
    mc.Clients.Store(client.Uuid, client)
    zap.L().Info("client registered", zap.String("userId", client.Uuid))
}

// UnregisterClient 注销客户端
func (mc *MsgConsumer) UnregisterClient(client *UserConn) {
    if client != nil {
        client.cleanup()
    }
}

// GetClient 获取客户端
func (mc *MsgConsumer) GetClient(userId string) *UserConn {
    value, ok := mc.Clients.Load(userId)
    if !ok {
        return nil
    }
    return value.(*UserConn)
}

// KickClient 单点登录互踢
func (mc *MsgConsumer) KickClient(userId string, reason string) {
    client := mc.GetClient(userId)
    if client != nil {
        // 推送下线通知
        notify := map[string]interface{}{
            "type":    message_type.KickNotification,
            "content": reason,
        }
        data, _ := json.Marshal(notify)
        _ = client.Conn.WriteMessage(websocket.TextMessage, data)
        // 断开连接
        client.cleanup()
    }
}

// Close 关闭资源
func (mc *MsgConsumer) Close() {
    mc.Reader.Close()
    mc.Producer.Close()
}
```

---

## 7. 在 main.go 中启动

项目启动时创建 `ChatServer`，初始化 Kafka，注入 Broker 到 `WsHandler`：

```go
// cmd/kama_chat_server/main.go

chatServer := chat.NewChatServer(chat.ChatServerConfig{
    MessageRepo:     repos.Message,
    FriendshipRepo:  repos.Friendship,
    GroupMemberRepo: repos.GroupMember,
    SessionRepo:     repos.Session,
    CacheService:    cacheService,
    UserRepo:        repos.User,
})
chatServer.InitKafka()

handlers := handler.NewHandlers(services, chatServer.GetBroker())
go chatServer.Run()
```

---

## ✅ 本节小结

- `ChatServer` 负责初始化和管理 Kafka 消息代理
- `MsgConsumer`（Kafka 模式）通过 Reader 从 Kafka 消费消息
- 所有依赖（Repo/CacheService）通过依赖注入进入 Broker
- 支持单点登录互踢（KickClient）和消息撤回通知（PushRecallNotify）

下一章进入消息处理细节：单聊/群聊的入库、转发、以及 Redis 缓存更新策略。
